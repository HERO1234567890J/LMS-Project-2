# ANALYSIS.md — المرحلة صفر (تحليل الوضع الحالي)

> ملخص كامل لتحليل `frontend/` كما هو موجود فعليًا في الكود، قبل أي بناء لاحق.
> كل قيمة مذكورة هنا تم استخراجها من الملفات الفعلية (HTML/CSS/JS) وليست افتراضات.

---

## 0) الحالة النهائية للمشروع (آخر تحديث — نهاية المرحلة الحادية عشرة)

| القطاع | الحالة |
|---|---|
| `backend` — البنية والـ API | مكتمل: Express + PostgreSQL (`pg`)، جلسات آمنة عبر `connect-pg-simple`، حماية أدوار (`admin`/`student`) وrate limiting. تعريف قاعدة البيانات موجود في `backend/src/schema.js` ولا توجد migrations منفصلة. |
| المصادقة والملفات الشخصية | مكتمل (تسجيل/دخول/خروج/نسيان/تغيير كلمة المرور/أجهزة) — المرحلة 1. |
| المحتوى التعليمي | مكتمل (فئات/مراحل/مواد/شهور/كورسات/دروس مع نشر وأرشفة وترتيب وبيع) — المرحلة 2. |
| الاشتراكات وأكواد التفعيل | مكتمل بالكامل: **المرحلة 3** (سجل مدفوعات، إلغاء) + **المرحلة 6** (توليد دفعات من أكواد قوية عشوائية مربوطة بالكورس/السعر/الصلاحية، قائمة بفلترة + ترقيم صفحات، تقرير المتاح لكل كورس، تفعيل يدوي بدون كود، استبدال الكود من الطالب بفحوص مرتبة في معاملة ذرّية، تقييد استبدال صارم لكل مستخدم وIP مع تدقيق كل محاولة فاشلة، قائمة اشتراكات الطالب النشطة). |
| الوسائط والملفات | مكتمل (رفع غلاف/فيديو/مرفقات بتحقق من المحتوى الفعلي، مكتبة وسائط، حذف آمن) — المرحلة 4. |
| نظام الامتحانات | مكتمل (مدرس: إنشاء امتحانات وأسئلة 3 أنواع، تصحيح مقالي، تعديل درجات نهائية؛ طالب: بدء/تسليم/تاريخ/مراجعة، اشتراك + فتح تسلسلي + حدود محاولات + فحص وقت خادمي) — المرحلة 5. |
| شجرة المحتوى | نظيفة ومنسقة: التعليمي → الفئات → المراحل → المواد → الشهور → الكورسات → الدروس → الامتحانات. |
| الجدولة والإشعارات | مكتمل: **المرحلة 7** — حقل `publish_at` موحّد على الكورسات/الدروس/الامتحانات، مجدول مشترك واحد ينشر تلقائيًا (نبضة أولى + كل 60 ثانية)، سجل تتبع `scheduled_publishes`، إشعارات داخلية (نشر محتوى / تفعيل اشتراك / استبدال كود / إرسال يدوي من المدرس)، قائمة وقراءة/إلغاء قراءة للطالب. |
| endpoints قراءة فقط (مدرس) | **المرحلة 8** — `GET /students` (بحث/فلترة/ترقيم صفحات)، `GET /students/:id` (ملف كامل: اشتراكات + محاولات + إحصاءات)، `PATCH /students/:id/status` (حظر/إلغاء مع قطع جلسات + منع الحظر الذاتي)، `GET /attempts` (نتائج بفلاتر)، `GET /stats` (عدادات + أحدث الطلاب)، `GET /exams` (قائمة مع عدد الأسئلة). |
| لوحة تحكم المدرس (`frontend/admin/`) | **المرحلة 8 — مكتملة**: 8 أقسام مربوطة بالـ API الفعلي (نظرة عامة، إدارة محتوى هرمية، إضافة محاضرة وامتحان، الطلاب، أكواد التفعيل، الامتحانات والدرجات، الإشعارات، الملف الشخصي) + مودالات (نموذج محتوى/تفعيل يدوي/توليد أكواد) + خروج/سمة داكنة/طباعة. |
| endpoints قراءة/تحديث (طالب) | **المرحلة 9** — `GET /courses` (كتالوج منشور غير مشترك به + أسماء مرحلة/مادة/شهر + عدد الدروس)، `GET /courses/:id` (تفاصيل كورس مشترك به + دروس `open/passed` + تقدم)، `GET /courses/:id/lessons/:lessonId` (الدرس كاملًا مع الوسائط والامتحانات وإحصاءات المحاولات)، `GET /attempts` (كل المحاولات بسياقها)، `GET/PATCH /profile` (عرض/تحديث ملف الطالب: رقم الطالب/ولي الأمر/المحافظة/تاريخ الميلاد/ملاحظات). |
| بوابة الطالب (`frontend/client/`) | **المرحلة 9 — مكتملة**: 5 أقسام مربوطة بالـ API الفعلي (الكورسات المتاحة + مودال استبدال كود، الكورسات المشترك بها مع فتح تسلسلي وتقدم، نتائج ومحاولات، الدعم الفني، الحساب الشخصي) + إشعارات + خروج فعلي. |
| الصفحات العامة (`frontend/index.html`) | **المرحلة 10 — مكتملة**: عرض المراحل الدراسية الحقيقية (قسم يُحقن عبر JS بنفس التصميم) + الكورسات المميزة المنشورة (`badge='featured'`) من قاعدة البيانات بالترتيب الصحيح بدل الكروت الثابتة + نموذج "تواصل معنا" موصول بـ endpoint يخزّن الرسالة + أزرار (سجّل/اشترك) بحفظ الوجهة المقصودة وإعادتها بعد الدخول/التسجيل. لا تغيير في التصميم البصري — JS فقط. |
| endpoints عامة (بدون جلسة) | **المرحلة 10** — `GET /api/courses/public/home` (مراحل منشورة بعدّاد كورسات + كورسات مميزة منشورة بأسماء المرحلة/المادة/الشهر) و`POST /api/courses/public/contact` (استقبال وتخزين رسالة التواصل في جدول `contact_messages` مع تقييد 10/ساعة/IP وتدقيق). |
| **تقارير المدرس + تصدير Excel** | **المرحلة 11 — مكتملة**: `/api/teacher/reports/revenue` (إيرادات حقيقية من الاشتراكات النشطة بـ COALESCE(ac.price, c.price) مع تقسيم حسب الفئة/المرحلة/المادة/الشهر/الكورس)، `/api/teacher/expenses` (POST سجل مصروف + GET قائمة بفلتر تاريخ)، `/api/teacher/reports/profit` (الإيرادات − المصروفات)، `/api/teacher/reports/export?type=students\|revenue\|subscriptions` (Excel عربي RTL عبر exceljs — ترويسة متميزة، أرقام كقيم رقمية، حقول تفقيط، audit). |
| **SEO** | **المرحلة 11 — مكتملة**: `/course/:id` (صفحة كورس مُنتجة من السيرفر بـ meta/canonical/OG/Twitter، تستبدل وتمنع زحف JS-only)، `/sitemap.xml` (ديناميكي للصفحات العامة + كل كورس منشور)، `/robots.txt` (يستثني `/admin` `/client` `/api` `/uploads` وصفحات الدخول)، حقن OG/canonical لكل صفحة عامة HTML (middleware يقرأ `PAGE_META` ويستبدل `</head>`). |
| **البحث الشامل** | **المرحلة 11 — مكتملة**: `/api/student/search?q=` (كورسات/دروس/امتحانات **مقيدة باشتراكات الطالب النشطة** فقط)؛ `/api/teacher/search?q=` (طلاب/كورسات/دروس/أكواد/اشتراكات/مصروفات عبر LIKE-escaped، تقييد لكل مستخدم). |
| **المراجعة الأمنية + الإصلاحات** | **المرحلة 11 — مكتملة**: مراجعة شاملة لـ 5 محاور (auth/SQL/IDOR/XSS/headers)، 4 إصلاحات دفاعية (cookie.secure من NODE_ENV + SESSION_SECRET required in prod + امتداد الرفع من magic bytes + global error handler + security headers)؛ 23/23 محاولة اختراق فعلية مرّت. التفاصيل في `SECURITY_REVIEW.md`. |
| البيانات | `users` id=1 مدير المنصة (admin/`admin123`)، id=2 أحمد محمد (student/`student123`)، id=3 محمود عبد (student/`student123`-المُعدَّل) — بيانات اختبار المرحلة 11 محفوظة لإظهار عمل التقارير/SEO/البحث: فئة تعليمية → 2 مرحلة → 2 مادة → 2 شهر → 2 كورس (4 منشورة نعلمها للميزانية) → 2 درس → 1 امتحان → 2 أكواد (1 مُستخدَم) → 2 اشتراك (1 كود + 1 يدوي) → 3 مصروفات (الإجمالي 680ج). |
| جودة التحقق | كل المراحل اختُبرت ضد الخادم الفعلي (محصلة المراحل 1–11: 34+32+34+31+135+31+31+31+31+17+23 سيناريو E2E على إصلاحات الأمان) مع `npm audit` (0 vulnerabilities) وكل commit صغير + مراجعة. |
| التوثيق | ANALYSIS.md (أقسام 1–17) + SCHEMA.md + SECURITY_REVIEW.md تقرير أمني شامل مع اختبار اختراق فعلي. |
| واجهات المستخدم (`frontend/`) | **لوحة المدرس `frontend/admin/` (المرحلة 8)** + **بوابة الطالب `frontend/client/` (المرحلة 9)** + **الصفحات العامة مربوطة بالبيانات الحقيقية (المرحلة 10)** + **محتوى خادمي للـ SEO (المرحلة 11)** — كل ذلك مكتمل. |

---

## 1) هيكل الملفات الحقيقي بعد التنظيف

```
LMS-Project/
├── ANALYSIS.md                  ← هذا الملف
├── backend/                     ← API (Node/Express + PostgreSQL) — يخدم الـ frontend نفسه (same-origin)
│   └── src/
│       ├── server.js            ← الدخول (port 8000) + إنشاء الـ schema و seed
│       ├── app.js               ← session middleware + الحماية بالدور + static للـ frontend
│       ├── db.js                ← اتصال PostgreSQL عبر pg وعمليات query/transaction
│       └── schema.js            ← تعريف الجداول وبيانات levels/terms الأولية
│       └── domains/             ← auth (شغال)، courses/students/exams (سكيلتون 501)
├── frontend/                    ← الموقع كله (static) — يقدَّم عبر الـ backend على http://localhost:8000
│   ├── index.html               ← الصفحة الرئيسية (Landing)
│   ├── about.html               ← صفحة "من أنا؟"
│   ├── login.html               ← تسجيل الدخول/إنشاء حساب (جديد، بتصميم البراند)
│   ├── admin/                   ← لوحة تحكم المدرس (role=admin)
│   │   ├── index.html           ← (logout + اسم المستخدم من الجلسة)
│   │   ├── style.css
│   │   ├── teacher-script.js    ← (تحقق الجلسة + logout)
│   │   └── WhatsApp Image 2026-07-02 at 10.08.43 PM.jpeg   ← صورة غلاف كورس تجريبية
│   ├── client/                  ← بوابة الطالب (role=student)
│   │   ├── index.html           ← (اسم المستخدم من الجلسة)
│   │   ├── script.js            ← (تحقق الجلسة + logout)
│   │   └── style.css
│   ├── assets/
│   │   ├── css/                 ← main.css، mainabout.css (مستخدمة) + admin.css، client.css، stylelog.css (ميتة/متروكة)
│   │   ├── fonts/               ← Mosvita-Medium.ttf، Mosvita-BoldExpanded.ttf
│   │   ├── img/                 ← about, illustration, logo, misc, person, portfolio, services
│   │   ├── js/                  ← main.js (مستخدمة) + admin.js، client.js، moderator.js (ميتة/متروكة)
│   │   └── vendor/              ← bootstrap, aos, swiper, glightbox, isotope, purecounter, imagesloaded, php-email-form
│   └── .gitignore               ← Thumbs.db, desktop.ini, .vscode/, .idea/, *.zip
```

**تنظيف تم إنجازه:**
- حُذف `.git` الفرعي داخل `frontend/`؛ أصبح مستودع واحد على مستوى `LMS-Project/`.
- أُزيلت الفولدرات المكررة: `frontend/admin/admin/*` → `frontend/admin/` و`frontend/client/client/*` → `frontend/client/` (مع حذف `.vscode/` الفارغ المتخلف من الزيب).
- بعد النقل: كل المراجع الداخلية (style.css، script.js، teacher-script.js، صورة الغلاف) لا تزال سليمة لأنها كانت نسبية لنفس المجلد.

---

## 2) الصفحات والمكونات الموجودة فعليًا

### 2.1 الصفحات العامة (Public)

**`index.html` — الصفحة الرئيسية (Landing)**
- موقع تسويقي لمدرّس اللغة العربية (إعدادي + ثانوي عام) — كان يحمل اسم مدرّس حقيقي في المحتوى الأصلي؛ أُزيل هنا لأن الفرونت إند القديم بالكامل حُذف واستُبدل.
- `lang="en"` على وسم `<html>` لكن المحتوى عربي، مع `dir="rtl"` على الأقسام.
- أقسام: Hero + عن منصتنا (فيسبوك فيديو) + الدورات المميزة (4 كروت: النحو 150ج، البلاغة 180ج، الأدب 150ج، مراجعة نهائية 180ج) + تواصل معنا (نموذج) + فوتر.
- مكتبات: Bootstrap 5 + Bootstrap Icons + AOS + Swiper + GLightbox + Owl Carousel (jQuery).
- **روابط مكسورة:** `login.html` (سطر 87)، `client.html` (سطر 78)، و`forms/contact.php` (سطر 552) — لا يوجد أي منها.
- بيانات تواصل حقيقية: تليفون `01060021497`، سوشيال ميديا (فيسبوك/تيك توك/يوتيوب).
- "تصميم وتطوير: ENG - HASSAN MOHAMED".

**`about.html` — صفحة "من أنا؟"**
- `lang="ar" dir="rtl"`.
- نبذة عن المدرس + شبكة مهارات + تخطيط زمني + اقتباس + حقائق سريعة.
- تستخدم `mainabout.css` وهي نسخة مخصصة من قالب BootstrapMade "Visible".
- **ملاحظة محتوى:** التخطيط الزمني والوصف يحكيان عن مدرس "فيزياء" (نصوص منسوخة لم تُحدّث بالكامل)، والفوتر يحمل حقوق "HA" المتبقية من القالب.
- **مراجع مكسورة:** `assets/img/favicon.png`، `assets/img/apple-touch-icon.png`، `assets/vendor/typed.js/typed.umd.js`، `assets/vendor/waypoints/noframework.waypoints.js` — غير موجودة (غير حرجة؛ أيقونة + مكتبات JS غير مستخدمة).

### 2.2 لوحة تحكم المدرس — `admin/` (Teacher Dashboard)

- `lang="ar" dir="rtl" data-theme="light"` — SPA بستة أقسام يُنقل بينها من السايدبار:
  1. **نظرة عامة والإحصائيات** — إحصائيات ثابتة (1,240 طالب، 6 كورسات، 350 كود) + جدول آخر الطلاب.
  2. **إدارة الكورسات** — كروت الكورسات + مودال "إضافة كورس" مع تدرّج مرحلة ← صف (بيانات الصفوف في `teacher-script.js`).
  3. **إضافة محاضرة وامتحان** — نموذج محاضرة (فيديو/مرفقات/رفع ملفات) + منشئ أسئلة ديناميكي + إعدادات امتحان (وقت/نسبة نجاح).
  4. **إدارة الطلاب والأكواد** — بحث + جدول طلاب + كارت ملف طالب (يُملأ من صف الجدول) + مودال توليد أكواد تفعيل مع طباعة PDF.
  5. **تقارير ورصد الدرجات** — فلترة (صف/امتحان) + جدول نتائج + مودال "كراسة إجابة الطالب".
  6. **إعدادات الحساب** — بيانات المعلم (اسم "حسن محمد"، واتساب `01027519267`) + تغيير كلمة المرور.
- سلوكها: **كله Mock/ثابت في الواجهة** — بيانات HTML مكتوبة يدويًا، ولا يوجد أي اتصال Backend. الفلاتر والتوليد والبحث كلها JavaScript على DOM فقط.
- بتبديل theme (ليلي/نهاري) عبر `localStorage('theme')`.
- ملاحظة: افتراضية المعلم "مدرس اللغة الإنجليزية" (بيانات وهمية) رغم أن الموقع الأساسي عربي — بيانات عرض توضيحي.

### 2.3 بوابة الطالب — `client/` (Student Dashboard)

- `lang="ar" dir="rtl" data-theme="light"` — SPA بخمسة أقسام:
  1. **الكورسات المتاحة** — كروت أشهر + مودال إدخال "كود تفعيل من 12 رقم" (التحقق مجرد `alert` وهمي).
  2. **الكورسات المشترك بها** — تنقّل Drill-down من 4 مستويات:
     - مستوى الكورسات → محاضرات (قائمة تُولّد من `lecturesData` في `script.js`، المحاضرة التالية مقفولة حتى اجتياز الامتحان بـ ≥50%)
     - → تبويبات المحاضرة (فيديو / ملفات PDF / امتحان) → واجهة امتحان فعلي (عدّاد 5 دقائق، شبكة أسئلة، تنقّل).
     - الامتحان الحقيقي `mockQuestions` من 3 أسئلة إنجليزية (بيانات تجريبية).
  3. **نتائج الامتحانات** — إحصائيات + كروت امتحانات + مراجعة منزلقة سؤال-بسؤال (صحيح/خطأ بالألوان).
  4. **الدعم الفني** — واتساب `https://wa.me/201027519267` + تيليجرام + FAQ (أكورديون) + نموذج تذكرة (وهمي).
  5. **الحساب الشخصي** — بيانات (رقم الطالب/ولي الأمر/المحافظة dropdown كامل) + تغيير كلمة مرور + زر تسجيل خروج (**بلا أي منطق فعلّي**).
- سلوكه: **كله Mock/ثابت في الواجهة** — `localStorage` للـ theme فقط، لا يوجد اتصال Backend ولا تحقق حقيقي من أكواد التفعيل.
- المستخدم الافتراضي الثابت: "أحمد محمد".

### 2.4 الملفات المتروكة (Orphaned / من نسخة سابقة) — لم تُحذف، وثّقت فقط

الملفات التالية **غير مُشار إليها من أي صفحة HTML حالية** لكنها تحمل منطقًا مهمًا من نسخة قديمة:

| الملف | ماذا كان |
|---|---|
| `assets/js/admin.js` | لوحة Admin قديمة تتصل بـ `http://127.0.0.1:8000/api` (Bearer token)؛ حماية `role !== "Admin"` → redirect لـ `login.html` |
| `assets/js/client.js` | بوابة طالب قديمة تتصل بنفس الـ API؛ (`/api/courses`، `/api/my-courses`، enroll) |
| `assets/js/moderator.js` | لوحة Moderator قديمة؛ (`/api/moderator/courses` CRUD)، redirect لـ `client.html` لو الدور غير Moderator |
| `assets/css/admin.css` | ستايلات لوحة Admin القديمة (نفس ثيم Indigo بنظام variables + dark mode) |
| `assets/css/client.css` | ستايلات بوابة الطالب القديمة |
| `assets/css/stylelog.css` | **ستايلات صفحة Login/Signup** (فلاب كارد "Log in / Sign up") — لا يوجد لها صفحة |

> الاستنتاج: في نسخة أقدم كان الموقع كاملًا (login.html + client.html + لوحات متصلة بـ Backend) لكن النسخة الحالية `Test-Platform-main` استبدلت كل ده بواجهات Static فقط.

---

## 3) مكان منطق تسجيل الدخول (Login) — الإجابة المباشرة

**لا يوجد أي صفحة أو منطق تسجيل دخول فعّال في النسخة الحالية.**

- زرار "تسجيل الدخول" في `index.html` (سطر 87) يربط لـ **`login.html` غير الموجود**.
- زرار "حسابي" (سطر 78) يربط لـ **`client.html` غير الموجود**.
- داخل `client/` و`admin/` **لا يوجد أي تحقق تسجيل دخول** — الصفحتان تُفتحان مباشرة بمستخدمين ثابتين (أحمد محمد / حسن محمد)، وزر "تسجيل الخروج" في `client/index.html` بلا أي منطق (لا حذف session ولا redirect).
- **البصمة الوحيدة على منطق login قديم** في `assets/js/admin.js` و`client.js` و`moderator.js`: قراءة `localStorage.getItem("token")` و`("role")`، وredirect قسري لـ `login.html` لو غابت، ونقاط API مفترضة (`/api/login`، `/api/logout`) على `http://127.0.0.1:8000` (نظام Laravel-API على الأغلب، أدوار: `Admin` / `Moderator` / `Student`).
- `assets/css/stylelog.css` = التصميم القديم لصفحة login (فلاب كارد).

**الفرق عن الخطة الأصلية:** الخطة كانت تتوقع ملف `login.html` منفصل أو login داخل `client` — الواقع أن الاثنين غائبان تمامًا من النسخة النشطة؛ الـ login موجود فقط كأثر CSS/JS يتيم في `assets/`.

---

## 4) Design Tokens المستخرجة (من الكود الفعلي)

### 4.1 الثيم العام — الصفحات العامة (index/about عبر `main.css` و`mainabout.css`)

**ثيم "ذهبي/جملي/كريمي"** (تعليقات الكود تؤكد: هوية ذهبية فاخرة):

| Token | القيمة | المعنى |
|---|---|---|
| `--background-color` | `linear-gradient(135deg, #FFFFFF 0%, #F5EFE6 100%)` | خلفية عامة بيضاء→كريمي |
| `--default-color` | `#4A3B2C` | لون النص الأساسي (بني) |
| `--heading-color` | `#2C1E0F` | العناوين (بني محروق داكن) |
| `--accent-color` | `#D4AF37` | الذهبي (أزرار/روابط/تنبيهات) |
| `--surface-color` | `#FFFFFF` | البطاقات |
| `--contrast-color` | `#FFFFFF` | نص فوق الذهبي |
| `.light-background` | `linear-gradient(135deg, #FFFFFF 0%, #FFF4D2 100%)`، `--surface-color: #FDFBF7` | خلفية أقسام فاتحة |
| `.dark-background` | `linear-gradient(135deg, #A87C38 0%, #C29B62 100%)`، `--surface-color: #8E652A`، `--contrast-color: #2C1E0F` | أقسام داكنة |
| hover الذهبي | `#A87C38` (جملي داكن) | زر login `.login-btn` |
| كروت الدورات | border `#F5EFE6`، shadow `0 2px 10px rgba(0,0,0,0.05)`، hover border `#A87C38`، hover bg `#FDFBF7` | زر `.btn-gold` bg `#D4AF37` |

- **الخطوط (الصفحة العامة):** `Cairo` (Google Fonts) هو الخط الفعلي المعروض (يُفرض عبر inline style في index.html مع استثناء الأيقونات). المتغيرات في `main.css` ما زالت من القالب (`Roboto`/`Abel`/`Mukta`) لكنها **غير مستخدمة فعليًا** في العرض. خط محلي `Mosvita` و`MosvitaBoldExpanded` (فولدر `assets/fonts/`) لاستخدامات الأرقام/النصوص الخاصة.
- **RTL:** المحتوى العربي مع `dir="rtl"` على الأقسام (index)، و`dir="rtl"` كامل على الصفحة (about).

### 4.2 الثيم المشترك — لوحات Admin و Client (داخل `admin/style.css` و`client/style.css`)

**القرار (بتوافق المستخدم):** توحيد لوحتي Admin وClient على برومت البراند الأصلي (ذهبي/كريمي/بني) بدل Indigo — طُبّق فعليًا على الألوان الثابتة والمتغيرات في الملفين.

| Token | Light | Dark | ملاحظة |
|---|---|---|---|
| `--primary` | `#D4AF37` | (ثابت) | الذهبي (أزرار/روابط/تمييز) |
| `--primary-hover` | `#B8912E` | (ثابت) | ذهبي داكن |
| `--bg-main` | `#F5EFE6` | `#1A1206` | كريمي / بني داكن جدًا |
| `--bg-card` | `#FFFFFF` | `#2C1E0F` | أبيض / بني محروق |
| `--bg-sidebar` | `#2C1E0F` (ثابت) | | بني محروق داكن في الوضعين |
| `--text-main` | `#2C1E0F` | `#F5EFE6` | العناوين (بني محروق) / كريمي |
| `--text-muted` | `#8A7B5E` | `#B5A488` | بني هادئ |
| `--text-sidebar` | `#F5EFE6` (ثابت) | | كريمي |
| `--border-color` | `#E7D8BC` | `#4A3B2C` | كريمي / بني |
| `--success` | `#10b981` | | ألوان الحالة سليمة (لم تُمس) |
| `--warning` | `#f59e0b` | | |
| `--danger` | `#ef4444` | | |
| `--whatsapp` | `#25d366` | | |
| `--sidebar-width` | `260px` | | |
| `--radius` | `12px` | | زوايا الكروت |
| `--transition` | `all 0.3s ease` | | |

> التدرجات الثانوية المستخدمة: كريمي فاتح `#FAF5EA` (خلفيات فرعية)، كريمي `#F4ECDC` (تاجات/أيقونات)، حدود `#D9C9A8`، أزرق/بنفسجي تحوّل لذهبي (`#3b82f6`/`#9333ea`/`#2563eb` → `#B8912E`)، بني نص متوسط `#6B5B43`. الوضع الليلي تحول لطيف بني دافئ بدل slate.

- **الخط:** `Cairo` حصريًا (`font-family: 'Cairo', sans-serif` على `*`).
- **RTL:** `body { direction: rtl }` — السايدبار يمين، الجداول `text-align: right`.
- **أزرار:** `.btn-primary` = indigo؛ `.btn-secondary` = `#64748b`؛ `.btn-success` = `#10b981`؛ `.btn-danger` = `#ef4444`؛ `.btn-outline` = border `--border-color`؛ `.btn-whatsapp` = `#25d366`؛ `.btn-telegram` = `#0088cc`؛ radius 8px، وزن 700 (admin) / 600 (client).
- **كروت:** bg `var(--bg-card)`، border `var(--border-color)`، radius `var(--radius)`، padding 1.5rem.
- **ألوان حالة إجابات الامتحان:** صح = `#dcfce7`/`#15803d`/border `#86efac`؛ خطأ = `#fee2e2`/`#dc2626`/border `#fca5a5`.
- **عناصر إضافية في client:** `.stat-num` و`.box-title` بلون برتقالي `#d97706`؛ `.btn-blue` = `#2563eb`؛ `.btn-green` = `#16a34a`؛ التليجرام `#0088cc`.

---

## 5) ملاحظات وفروقات تستحق الانتباه (مقارنة بالخطة الأصلية)

1. **Login/Backend كانا مفقودين — **تم حلهما في المرحلة 1**:** الموقع كان كله Static/عرضي ورابط `client.html` مكسور. الآن يوجد Backend (Express+PostgreSQL) بمصادقة حقيقية (سشنز/كوكيز) وصفحة `login.html`، والـ backend يخدم كل الصفحات. رابط "حسابى" يذهب إلى `/client/` (يُعاد توجيهه لـ login لو غير مسجّل).
2. **نموذج الاتصال يرسل لـ `forms/contact.php` غير الموجود** — النموذج لا يعمل فعليًا.
3. **النسخة القديمة المتروكة في `assets/`** تحدد شكل الـ API والـ Auth المتوقع مستقبلًا (REST على `127.0.0.1:8000/api`، Bearer token، أدوار Admin/Moderator/Student) — مصدر مهم لتصميم الـ Backend الجديد، لكن **لا يجب اعتماده كمرجع تصميم معمارى نهائي دون موافقتك**.
4. **عدم اتساق الهوية البصرية — **تم حله**:** لوحات admin/client كانت Indigo وبقيت الصفحات العامة ذهبية/كريمية. **قرار المستخدم:** توحيد الجميع على برومت البراند الأصلي (ذهبي `#D4AF37`، كريمي `#F5EFE6`، عناوين بني محروق `#2C1E0F`، نص `#4A3B2C`، خط Cairo). طُبّق على كل الألوان الثابتة والمتغيرات في `admin/style.css` و`client/style.css` (في commit منفصل). الوضع الليلي بقي لكن بأطياف بنية دافئة بدل slate.
5. **الموقع الفعلي كان لمدرّس لغة عربية حقيقي** (تم إزالة اسمه من التوثيق بعد حذف الفرونت إند القديم بالكامل) لكن لوحة المدرس كانت تُعرض بمستخدم "حسن محمد — مدرس إنجليزية" وامتحاناتها بالإنجليزية — بيانات عرض توضيحية لم تُعدل.
6. **محتوى `about.html` فيه نصوص فيزياء غير مُحدّثة** (منسوخة من قالب) + حقوق "HA".
7. **الأسعار في index.html** ثابتة (150/180 جنيه) — ليست مصدر موثوق؛ الأفضل أن تأتي من Backend لاحقًا.
8. **أكواد التفعيل (12 رقم) في client** مفهوم تجاري محوري في المشروع (توليد/طباعة PDF من admin، إدخال من client) — لم يُنفذ تحققها الحقيقي بعد.
9. **المراجع المكسورة غير الحرجة:** favicon + `typed.js`/`waypoints` في about.html.
10. **البنية المستوية:** الصفحات العامة في `frontend/` مباشرة، بينما لوحتا `admin/` و`client/` تحت مجلدات خاصة — يجب الحفاظ على هذا أو توثيق إعادة الهيكلة المقترحة قبل القيام بها.

---

## 6) سكيلتون الـ Backend (المرحلة صفر) ثم المصادقة (المرحلة 1)

- **الاختيار:** Node.js + Express + PostgreSQL (`pg`) — بنية منظمة حسب الدومين.
- **البنية:** `backend/src/server.js` (التطبيق والمسارات)، `backend/src/db.js` (اتصال PostgreSQL والمعاملات)، `backend/src/schema.js` (تعريف الجداول والبيانات الأولية).
- **التحقق:** `/health` ترجع `{"status":"ok"}`، وأي روت دومين غير منفذ يرجع 501. اشتغل بنجاح (بدء التشغيل يأخذ ~14 ثانية على هذا الـ mount البطيء).
- **المصادقة (مكتملة — قرار المستخدم: سشنز + كوكيز):**
  - الجلسات: `express-session` + `bcryptjs` + تخزين الجلسات في PostgreSQL عبر `connect-pg-simple` (كوكي httpOnly/sameSite=lax).
  - الدومين `auth`: `POST /api/auth/register` (student)، `POST /api/auth/login`، `POST /api/auth/logout`، `GET /api/auth/me`. أرقام الموبايل تُطبَّع لصيغة `+20xxxxxxxxx`. **(استُبدل في المرحلة 2 بدومينَي `teacher` و`student`، وحُذف `/api/auth` نهائيًا — انظر القسم 8.)**
  - جدول `users` (name/phone/password_hash/role) بأدوار `admin`/`moderator`/`student`.
  - الـ backend يخدم الـ frontend كاملاً (same-origin) → لا حاجة لـ CORS والكوكي يعمل مباشرة.
  - **الحماية بالدور:** `/admin/*` → role=admin فقط، `/client/*` → role=student فقط، غير مسجّل → redirect إلى `/login.html`.
  - **حساب أدمن مُهيّأ تلقائيًا:** الموبايل `01000000000` / كلمة المرور `admin123` (يُنشأ فقط لو الجدول فارغ — غيّره في الإنتاج).
  - **الواجهة:** صفحة `frontend/login.html` جديدة بتصميم البراند (دخول + إنشاء حساب)، تُوجّه حسب الدور. اللوحات تعرض اسم المستخدم الحقيقي من الجلسة وتنفّذ تسجيل الخروج فعليًا.
  - **خطوات تحقق تمت بـ curl:** register→login→me→guard بالدور→logout→401 بعدها. كلها سليمة.
- **بنية الداتابيز:** PostgreSQL، ويُنشأ schema من `backend/src/schema.js` عبر `npm run migrate`.
  - `users` — `id` INTEGER PK AUTOINCREMENT، `name` TEXT NOT NULL، `phone` TEXT NOT NULL UNIQUE، `password_hash` TEXT NOT NULL، `role` TEXT NOT NULL DEFAULT 'student' مع `CHECK (role IN ('admin','moderator','student'))`، `created_at` TEXT NOT NULL DEFAULT `datetime('now')`.
  - `sessions` — `sid` TEXT PK، `sess` TEXT NOT NULL (سلسلة الجلسة كاملة JSON)، `expires` INTEGER NOT NULL (نطبقها express-session store).
  - لا توجد جداول أخرى حاليًا (courses/students/exams domains مجرد روت 501 بلا جداول).
- **الحسابات الموجودة فعليًا في `lms.db` (آخر فحص 2026-08-16):**
  - `id=1` — **مدير المنصة** `+201000000000` — دور `admin` — (مُهيّأ تلقائيًا بالـ seed، كلمة المرور `admin123`).
  - `id=2` — **أحمد محمد** `+201012345678` — دور `student` — كلمة المرور `student123` (تم إنشاؤه عبر `POST /api/auth/register` أثناء اختبار المرحلة 1 — حساب اختباري).
  - `id=3` — **محمود عبد** `+201033334444` — دور `student` — إيميل `mahmoud@test.com`، ولي أمر `+201033334444` (أُنشئ أثناء اختبار المرحلة 2 — حساب اختباري فعّال).
  - حُذفت حسابات الاختبار المؤقتة التي أُنشئت أثناء التحقق من المرحلة 2 (كانت id=4..7).
  - كلمة مرور الأدمن غير مخزنة نصيًا (bcrypt hash) — لا يمكن استعادتها، فقط إعادة تعيين.
  - جدول جلسات PostgreSQL تتم إدارته بواسطة `connect-pg-simple`.
- **ملاحظة أمان للإنتاج:** هذه حسابات تجريبية فقط — يجب تغيير كلمة مرور الأدمن أو حذف الحسابات قبل النشر.
- **تحذير بيئة هام جدًا (9p mount على /mnt/c):**
  - لا توجد متطلبات بناء خاصة بقاعدة بيانات native؛ يعتمد backend على عميل PostgreSQL `pg`.
  - `git init`/`git config` تفشل أيضًا — استخدم الـ `.git` المُهيّأ والـ `-c user.name=...` في كل commit.
  - على جهاز عادي (Windows/macOS) `npm install` سيعمل بشكل طبيعي.
- **طريقة التشغيل الآن:** `cd backend && npm start` ثم افتح `http://localhost:8000` (الموقع + الـ API معًا).

---

## 7) خلاصة المرحلة صفر + المرحلة 1

- **المرحلة صفر (مكتملة):** النسخة الحالية كانت **أول نسخة مبيعات/عرض (Prototype static)** بلا مصادقة ولا تخزين. تم: توثيق التحليل، بناء سكيلتون Backend، توحيد الهوية البصرية على براند الذهبي/الكريمي.
- **المرحلة 1 (مكتملة — المصادقة):** Backend حقيقي (Express+PostgreSQL) بمصادقة سشنز/كوكيز + أدوار (`admin`/`student`) + صفحة login/register بتصميم البراند + الحماية بالدور على `/admin/*` و`/client/*` + تسجيل خروج فعلي + عرض اسم المستخدم الحقيقي من الجلسة في اللوحتين.
- **المتبقي لبناء LMS كامل:** ربط لوحات admin/client بالبيانات الحقيقية (كورسات، طلاب، امتحانات، نتائج، أكواد تفعيل)، وإنهاء دومينات courses/students/exams في الـ backend.

---

## 8) المرحلة 2 — تصميم قاعدة البيانات الكاملة (SCHEMA.md) + نظام المصادقة الحقيقي

### 8.1 القرارات المعمارية (بموافقة المستخدم)
1. **SCHEMA.md** — تصميم كامل لقاعدة البيانات (28 جدولًا) وُثّق واعتُمد قبل أي كود (commit `1f65191`).
2. **الجلسات:** سشنز سيرفرية قابلة للتجديد (أبقينا `express-session`، بلا JWT). التدوير = `req.session.regenerate()` عند كل دخول. الجلسات مخزنة في PostgreSQL عبر `connect-pg-simple`.
3. **الأدوار:** الـ Teacher = دور `admin` الحالي (المالك/المنشئ). الطالب = `student`.
4. **`email` على جدول `users`** (nullable، فهرس جزئي فريد `WHERE email IS NOT NULL`، تخزين lowercase) + جدول `password_reset_tokens` (token_hash فريد، expires_at 1 ساعة، used_at).
5. **إضافات أقرّها المستخدم:** `users.status` (افتراضي active) / `last_login_at` / `avatar_url`، و`student_profiles.guardian_phone`، و`sessions_devices.status` (active/suspicious/revoked) + `device_fingerprint`.

### 8.2 الترحيلات (Migrations)
- `backend/src/config/migrations.js` — مُشغّل ترحيلات مع جدول `schema_migrations`؛ migr 001 (baseline: users/sessions)، 002 (إضافة email/status/last_login_at/avatar_url + الفهرس الفريد الجزئي)، 003 (كامل SCHEMA.md: 24 جدولًا جديدًا + الإضافات)، 004 (password_reset_tokens).
- `backend/src/schema.js` — يحتوي على تعريف PostgreSQL schema، ويُنفذ عند الإقلاع وعبر `npm run migrate`.
- البيانات القائمة (admin + أحمد محمد) حُفظت سليمة عبر الترحيلات.

### 8.3 البنية المشتركة (`backend/src/lib/`)
- `auth.js` (تطبيع الموبايل +20، البحث بالمفردات، publicUser، hash/تحقق bcrypt، صلاحية كلمة المرور/الموبايل/الإيميل، requireAuth/requireRole) — `rateLimit.js` (نافذة منزلقة في الذاكرة؛ 429 برسالة عربية + Retry-After) — `mailer.js` (يُسجّل الإيميل في الكونسول حاليًا، قابل للاستبدال بـ SMTP) — `audit.js` (سجل تدقيق) — `sessionDevices.js` (تسجيل الأجهزة + تمييز المشبوهة + الإلغاء).

### 8.4 دومينات المصادقة
- **Teacher** (`/api/teacher/auth/*`): `login` (معدّل 5/15د، regenerate+save قبل تسجيل الجهاز، recordLogin، audit) — `logout` — `me` — `change-password` (يلغي الجلسات الأخرى) — `GET /api/teacher/sessions?student_id=` — `POST /api/teacher/sessions/:deviceId/revoke` (إدارة أجهزة الطلاب).
- **Student** (`/api/student/auth/*`): `register` (name/email/phone/guardian_phone/password + دخول تلقائي) — `login` (موبايل أو إيميل) — `logout` — `me` — `forgot-password` — `reset-password` (يلغي كل الجلسات ويميّز الأجهزة revoked) — `change-password` — `GET /api/student/auth/devices` — `POST /api/student/auth/devices/:deviceId/revoke`.
- حُذف `/api/auth` القديم نهائيًا (commit `bcc8110`).

### 8.5 قواعد الأمان والسلوك
- كل رسائل الخطأ عربية وغير كاشفة للتفاصيل؛ `forgot-password` لا يفضح وجود الإيميل (رسالة موحّدة).
- **عزل الدومين:** كوكي المعلم غير معروف لدومين الطالب (302 → `/login.html`) والعكس (302 → `/login-teacher.html`).
- `/admin/*` محمية عبر `pageGuard` → `/login-teacher.html`، و`/client/*` → `/login.html`.
- **إصلاح مهم:** بعد `session.regenerate()` لا يُحفظ الصف الجديد إلا عبر `req.session.save()` قبل إدراج الجهاز — بدونه كان يحدث خطأ FOREIGN KEY.
- **الأجهزة:** دخول من جهاز جديد يعلّم جلسات المستخدم القديمة `suspicious` حتى يتأكد منها المستخدم؛ `device_fingerprint` من user-agent/OS/browser/IP.

### 8.6 الواجهة الأمامية
- `login.html` أُعيدت كتابتها (طلاب: دخول/إنشاء حساب/نسيت كلمة المرور → `/api/student/auth/*`).
- `login-teacher.html` جديدة (صفحة دخول المعلم المخفية → `/api/teacher/auth/login`).
- `reset-password.html` جديدة (تقرأ `?token=` وتنفّذ reset-password).
- لوحتا admin/client: نقاط النهاية المحدّثة، تسجيل خروج فعلي، ونماذج تغيير كلمة المرور مربوطة بالـ backend.

### 8.7 التحقق العملي (E2E بـ curl — 27/27 نجحت)
- تسجيل طالب جديد (مع التحقق من تخزين guardian_phone)، الدخول بالموبايل/الإيميل، logout ثم 401 بعده، forgot/reset (استخراج التوكن من سجل الإيميل)، عزل الأدوار والدومينات، كشف الأجهزة (active=1 + suspicious)، إلغاء جلسة طالب من المعلم ثم 401، تغيير كلمة المرور، واختبار الـ rate limiting (429 على دخول الطالب/المعلم و forgot).
- الحسابات النهائية في `lms.db`: id=1 مدير المنصة (admin) — id=2 أحمد محمد (student) — id=3 محمود عبد (student، `mahmoud@test.com`، ولي أمر `+201033334444`). حُذفت كل حسابات الاختبار المؤقتة.

### 8.8 Commits
- تعريف schema الحالي موحد في `backend/src/schema.js` بدون مجلد migrations منفصل.
- `5c019e5` — البنية المشتركة + نظاما الكوكيز (student_sid/teacher_sid) + pageGuard.
- `e3a56fa` — دومين مصادقة المعلم (مع إصلاح FOREIGN KEY).
- `bcc8110` / `ee44f77` — دومين مصادقة الطالب + حذف `/api/auth`.
- `4038350` — صفحات المصادقة الأمامية (login/login-teacher/reset-password) + ربط اللوحتين.

**المتبقي (المرحلة 3+):** ربط لوحات admin/client بالبيانات الحقيقية وإنهاء دومينات courses/students/exams.

---

## 9) المرحلة 3 — API كامل للمحتوى التعليمي (محمي بمصادقة المدرس فقط)

### 9.1 المطلوب والمنجز
بُني API RESTful كامل (حماية `requireRole('admin')` على كل المسارات) يغطي الهرم التعليمي الستة بالترتيب: `educational_categories` → `stages` → `subjects` → `months` → `courses` → `lessons`. كل مستوى يدعم: إنشاء، تعديل، عرض قائمة مرتبة، تغيير حالة (draft/published/archived)، إعادة ترتيب، وحذفًا آمنًا.

### 9.2 مسارات الـ API (كلها تحت `/api/teacher/`)
| المستوى | إنشاء | قائمة | تعديل | حالة | إعادة ترتيب | حذف |
|---|---|---|---|---|---|---|
| categories | `POST /categories` | `GET /categories` | `PUT /categories/:id` | `PATCH /categories/:id/status` | `POST /categories/:id/reorder` | `DELETE /categories/:id` |
| stages | `POST /stages` | `GET /stages?category_id=` | `PUT /stages/:id` | ... | ... | ... |
| subjects | `POST /subjects` | `GET /subjects?stage_id=` | ... | ... | ... | ... |
| months | `POST /months` | `GET /months?subject_id=` | ... | ... | ... | ... |
| courses | `POST /courses` | `GET /courses?month_id=` | ... | ... | ... | ... |
| lessons | `POST /lessons` | `GET /lessons?course_id=` | ... | ... | ... | ... |

- **القائمة:** دعم فلترة بالأب (`?category_id=`/`?stage_id=`/`?subject_id=`/`?month_id=`/`?course_id=`) وبالحالة (`?status=`)، مع فرز `sort_order ASC, id ASC`. يبقى الربط بالأب عبر المفتاح الأجنبي صريحًا في كل صف.
- **الإنشاء:** يتطلب أبًا موجودًا (404 لو غاب)، `sort_order` تلقائي = أقصى ترتيب بين الإخوة +1 لو لم يُمرر، وكورسات فقط تحفظ `created_by` (المدرس المالك).
- **إعادة الترتيب:** `POST .../reorder` بـ `{ordering: n}` (موضع 1-أساسي) يُعاد ترقيم الإخوة داخل نفس الأب؛ يرفض خارج النطاق.

### 9.3 حقول المستويات الخاصة
- **courses (حقول الكورس المطلوبة):** `name_ar` (مطلوب)، `price` (مطلوب، صحيح ≥ 0، EGP)، `cover_url` (مسار ملف فقط — بلا رفع فعلي، يليها مرحلة وسائط لاحقة)، `description` (وصف مختصر)، `badge` (`normal`/`featured` — افتراضي normal).
- **lessons (حقول الدرس):** `name_ar` (مطلوب)، `youtube_url` (نص اختياري، يجب أن يبدأ بـ http(s):// — رابط يوتيوب كبيانات فقط)، `sort_order` (ترتيب داخل الكورس)، `status` + `publish_at` (تاريخ نشر مستقبلي كحقل بيانات فقط — **لا توجد أي جدولة تلقائية في الخلفية**، ستُنفذ في مرحلة نشر وجدولة لاحقة).
- **قاعدة البيانات:** migration `005` أضاف `courses.badge` (CHECK normal/featured) و`lessons.youtube_url` و`lessons.publish_at` — تكميل للأعمدة المشتركة المتفق عليها في SCHEMA.md (name_ar/description/cover_url/sort_order/status).

### 9.4 قواعد التعديل والحذف
- **التعديل** جزئي (يُحدَّث ما يُمرر فقط) + `updated_at = datetime('now')`؛ يُرفض التعديل الفارغ (400) والمفقود (404). لا يُغيَّر الأب (نقل عنصر بين آباء غير متاح في هذه المرحلة — محسوبًا).
- **الحذف الآمن:** يُفحص كل مستوى عما يشير إليه قبله:
  - كل مستوى يرفض الحذف إن وُجد أبناء تحته (category→stages، stage→subjects، subject→months، month→courses، course→lessons) برسالة 409 عربية تقترح الأرشفة (`suggestion: "archive"`).
  - **courses** يرفض أيضًا الحذف إن وُجدت **اشتراكات** (أي صف في `subscriptions`) — لا حذف لأي كورس مرتبط بمشتركين فعليين.
  - **lessons** يرفض الحذف إن وُجدت مرفقات (`lesson_assets`) أو امتحانات (`exams`).
  - القيود مُنفَّذة في طبقة الـ API؛ وعلى مستوى قاعدة البيانات تعمل `ON DELETE RESTRICT` كشبكة أمان (مؤكد عمليًا: حذف مرحلة فيها مواد فشل بـ FOREIGN KEY constraint failed).
- كل العمليات (إنشاء/تعديل/حالة/ترتيب/حذف) تُسجَّل في `audit_logs` مع before/after JSON وip.

### 9.5 البنية التنفيذية
- وحدة واحدة مدفوعة بالإعدادات: `backend/src/domains/teacher/content.js` (جدول LEVELS يصف لكل مستوى: الجدول، الأب، `created_by`، الحقول الخاصة، وما يشير إليه) — تُركَّب في دومين المدرس (`router.use('/', contentRoutes)`) بحماية `requireRole('admin')` عامة على الوحدة كلها. بلا تكرار كود عبر المستويات.
- الاستجابة: مفردة `{item}`، قائمة `{items}`، عمليات `{message}`، الأخطاء عربية وغير كاشفة للتقنيات.

### 9.6 التحقق العملي (E2E بمحاكاة تدفق حقيقي — 67/67 نجحت)
1. مدرس يسجّل دخول → 2. فئة تعليمية "تعليم أساسي" (منشورة) → 3. مرحلة إعدادية تحتها → 4. مادة "اللغة العربية" → 5. شهر "الشهر الأول" → 6. كورسان تحت الشهر (كورس النحو 150ج `featured` / كورس البلاغة 180ج `normal`) → 7. درسان تحت كورس النحو (الأول برابط يوتيوب، الثاني بتاريخ نشر مستقبلي).
8. **تعديل ترتيب الدروس:** نقل الدرس الأول للموضع 2 → أصبح الثاني أولًا ثم رجوعه للموضع 1.
9. **حذف آمن:** رفض حذف الفئة (لها مرحلة)، رفض حذف الشهر (له كورسات)، رفض حذف الكورس (له دروس)، رفض حذف درس (لما أُضيفت له مرفقات)، رفض حذف كورس بعد إضافة اشتراك (رسالة تذكر "اشتراكات") — كلها 409 مع اقتراح الأرشفة؛ وحذف درس بلا محتوى تحته نجح.
- فحوصات إضافية ناجحة: عزل الأدوار (طالب → 401)، التحقق من الحقول (اسم فارغ/سعر سالب/شارة غير صالحة/رابط غير http/تاريخ خاطئ → 400)، الربط بالأب في القوائم، الفلترة بالحالة، سلامة قاعدة البيانات بعد الحذف المرفوض، والتنظيف الكامل بعد الاختبار (كل الجداول رجعت فارغة، وحُذفت حسابات الاختبار المؤقتة).

### 9.7 Commits
- `10530aa` — migration 005 (courses.badge + lessons.youtube_url/publish_at).
- `3566236` — وحدة محتوى المرحلة 3 (كل المستويات CRUD+status+reorder+حذف آمن) + التركيب.
- `129c57c` — إصلاح تحقق الـ enum (قبول Array و Set) ظهر أثناء الاختبار.

### 9.8 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- **جدولة النشر التلقائي** (`scheduled_publishes`/خلفية فعلية) ستُنفذ في مرحلة نشر وجدولة لاحقة — الآن `publish_at` حقل بيانات فقط.
- **تغيير الأب** (نقل عنصر بين آباء) غير مفعّل في هذه المرحلة.
- **ربط واجهات admin/client بالـ API الجديد** لم يُنفذ بعد — هذه المرحلة API فقط.

---

## 10) المرحلة 4 — رفع الملفات وإدارة الوسائط (محمي بمصادقة المدرس فقط)

### 10.1 المطلوب والمنجز
بُنيت طبقة تخزين مجردة (قرص محلي الآن، قابلة للتبديل سحابيًا لاحقًا دون تغيير باقي الكود) وخمسة مسارات وسائط كلها `requireRole('admin')`: رفع غلاف كورس، رفع فيديو درس كملف مباشر (بديل لرابط يوتيوب — الاثنان مسموحان معًا)، رفع مرفقات متعددة لدرس، حذف ملف وسائط (يرفض إن كان مستخدمًا فعليًا في محتوى منشور)، وقائمة مكتبة الوسائط (كل الملفات المرفوعة مع نوعها وحجمها ومكان استخدامها).

### 10.2 بنية التخزين (`backend/src/lib/storage.js`)
- واجهة واحدة فقط: `saveFile({fileType, buffer, originalName, mime})` → `{url}`، `deleteFile(url)`، `fileExists(url)`، `getPublicUrl(diskPath)` — كلها async، والمسار الفعلي على القرص (تحت `backend/uploads/{covers|videos|files}/`) **مخفي داخل الخدمة**؛ قاعدة البيانات تحفظ `url` العام فقط (`/uploads/...`)، والتبديل السحابي لاحقًا يعني تغيير داخل الخدمة فقط.
- `app.js` يقدّم `app.use('/uploads', express.static(uploadsRoot))`، والمجلد مُستثنى في `.gitignore`.
- **أمان مسار:** `toDiskPath` يرفض أي url يخرج خارج `UPLOADS_ROOT` (يمنع path traversal — مؤكد عمليًا)، و`deleteFile` متكرر الأمان (idempotent) — لا يفشل لو الملف غير موجود.

### 10.3 قاعدة البيانات
- migration `006` أنشأ `media_library` (مصدر مكتبة الوسائط): `id`, `file_type` (`cover`/`video`/`attachment`), `url`, `mime`, `size_bytes`, `entity_type` (`course`/`lesson`), `entity_id`, `created_by`, `created_at`.
- **مرفق الدرس:** الفيديو والمرفقات تُحفظ كصفوف في `lesson_assets` (الجدول المتفق عليه في SCHEMA.md): `asset_type` (`video`/`file`), `provider='upload'`, `title`, `url`, `mime`, `file_size`, `sort_order` تلقائي (أقصى ترتيب +1). الغلاف يُحدَّث في `courses.cover_url`.
- الغرض من `media_library`: تخزين نوع/حجم/مكان الاستخدام لكل ملف مرفوع (الغلاف ليس له أعمدة حجم/نوع في `courses`)، ويكون مصدر قائمة المكتبة وفحص أمان الحذف.

### 10.4 مسارات الـ API (كلها تحت `/api/teacher/`، multipart)
| المسار | الوصف |
|---|---|
| `POST /courses/:id/cover` | رفع غلاف (حقل `cover`). يتحقق من المحتوى الفعلي (magic bytes) وحجمه، يحفظ، يحدّث `cover_url`، ويحذف الغلاف القديم من القرص والمكتبة تلقائيًا عند الاستبدال. |
| `POST /lessons/:id/video` | رفع فيديو مباشر (حقل `video`) → صف في `lesson_assets` (`asset_type='video'`، `provider='upload'`). |
| `POST /lessons/:id/attachments` | رفع مرفقات متعددة (حقل `attachments`، حتى 10) → صف لكل ملف في `lesson_assets` (`asset_type='file'`). |
| `DELETE /media/:id` | حذف ملف وسائط من المكتبة + القرص، مع إزالة مراجعه (مسح `cover_url` أو صف `lesson_assets`). |
| `GET /media` | قائمة كل الملفات: `file_type`, `url`, `mime`, `size_bytes`, مكان الاستخدام (`entity_type`/`entity_id`/`entity_name` من اسم الكورس أو الدرس), `created_at`. |

### 10.5 قواعد التحقق والحذف
- **التحقق من النوع الفعلي (وليس mimetype المُرسل فقط):** فحص magic bytes — الغلاف: JPG/PNG/WebP/GIF؛ الفيديو: MP4 (ftyp) / WebM (EBML)؛ المرفقات: PDF (`%PDF`) وZIP-docs (`PK`) وامتدادات مسموحة (pdf/docx/doc/pptx/ppt/xlsx/xls/txt/zip).
- **حدود الحجم:** غلاف 5MB، فيديو 100MB، مرفق 20MB (أخطاء multer → 400 عربي). عند فشل تحقق أي ملف في دفعة مرفقات لا يُحفظ أي ملف (لا ملفات يتيمة على القرص — مؤكد عمليًا).
- **حذف آمن:** أي ملف مستخدم فعليًا في **محتوى منشور** يُرفض حذفه (409 + `suggestion: "archive"`): غلاف كورس `status='published'` أو مرفق/فيديو لدرس `status='published'`. عند السماح بالحذف تُزال المرجعية ثم يُحذف الملف من القرص.
- كل العمليات (رفع غلاف/فيديو/مرفقات/حذف وسائط) تُسجَّل في `audit_logs`.

### 10.6 اختبار عملي (ضد الخادم الفعلي، ملفات حقيقية على القرص)
**34/34 سيناريو ناجح** تشمل: عزل المصادقة (401 بدون جلسة)، رفع غلاف فعلي لكورس + تحقق `cover_url` ووجود الملف على القرص، رفض صورة مزيّفة، رفع فيديو فعلي لدرس + صف `lesson_assets`، رفض فيديو مزيّف، رفع 3 مرفقات لدرس واحد + صف لكل ملف، رفض مرفق مزيّف (PDF مزيّف + امتداد غير مسموح) بدون ملفات يتيمة، قائمة المكتبة (5 ملفات مع مكان الاستخدام)، **استبدال الغلاف وحذف القديم فعليًا من القرص** مع بقاء صف غلاف واحد بالمكتبة، **رفض حذف فيديو/غلاف مستخدمين في محتوى منشور (409)** مع بقائهما على القرص، حذف ناجح بعد الأرشفة مع إزالة الملف من القرص ومسح المرجع، 404 لملف غير موجود، وتنظيف كامل (كل الجداول رجعت فارغة ولا ملفات متبقية في `uploads/`).

### 10.7 Commits
- `d92b3f6` — تثبيت `multer` (multipart).
- `c399144` — migration 006 (جدول `media_library`).
- `f2dc9be` — خدمة التخزين المجردة + مسار `/uploads` + gitignore.
- `827c5e1` — endpoints الوسائط (غلاف/فيديو/مرفقات/حذف/مكتبة) + التركيب.

### 10.8 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- **التخزين السحابي** (S3/حاليًا) غير منفذ — الواجهة المجردة جاهزة والتبديل داخل `lib/storage.js` فقط.
- **استمرار فيديو طويل بشكل متقطع** (تحميل جزئي/resume) و**معالجة الفيديو** (transcoding) خارج النطاق — الآن رفع مباشر كامل للملف.
- **ربط واجهات admin/client** (شاشة مكتبة الوسائط / رفع من النماذج) لم يُنفذ بعد — هذه المرحلة API فقط.

---

## 11) المرحلة الخامسة — نظام الامتحانات

نظام امتحانات كامل: المدرس يُنشئ الامتحانات والأسئلة (3 أنواع) ويصحّح المقالي ويمكنه تعديل الدرجة النهائية، والطالب يبدأ ويُسلّم ويعرض تاريخه ومراجعته — مع فوترة قواعد واضحة للاشتراك والفتح التسلسلي للدروس والمحاولات.

### 11.1 قرارات التصميم (اعتمدها المستخدم صراحةً عبر الاستبيان)
- **قرار أ — إعادة المحاولة:** عمود جديد `exams.allow_retry` (`0/1`). `allow_retry=0` ⇒ محاولة واحدة فقط. `max_attempts` يبقى (`NULL` = بلا حد، `N` = إجمالي المحاولات المسموحة) ويُجبر على `NULL` عندما تكون `allow_retry=0`.
- **قرار ب — حالة فتح الدروس:** جدول جديد `student_lesson_progress(student_id, lesson_id, exam_attempt_id, passed_at, created_at)` بفهرس فريد `(student_id, lesson_id)`. الدرس مفتوح = **أول درس في الكورس** OR **الطالب ناجح فيه بالفعل** OR **الطالب ناجح في الدرس الذي يسبقه مباشرة** (حسب `lessons.sort_order`). عند رسوب الدرس يُحذف صف التقدم الذي فتحه هذا الدرس (إعادة تصحيح لأسفل تُغلق الدرس الذي يليه).
- **قرار ج — عدة امتحانات لنفس الدرس:** نجاح الطالب في **أي** امتحان من امتحانات الدرس = نجاح في الدرس (يفتح التالي)؛ تكرار النجاح لا يُكرر الصف (INSERT OR IGNORE).

### 11.2 تغييرات قاعدة البيانات (migration `007`)
- `ALTER TABLE exams ADD COLUMN allow_retry INTEGER NOT NULL DEFAULT 0`.
- جدول `student_lesson_progress` + فهرس فريد `idx_student_lesson_progress_unique(student_id, lesson_id)` وفهرس على `student_id`. علاقة المفتاح الخارجي: `exam_attempt_id → exam_attempts(id) ON DELETE CASCADE` (حذف محاولة يزيل تقدمها المرتبط).
- بقية جداول الامتحانات (`exams`, `questions`, `question_options`, `question_images`, `exam_attempts`, `answers`, `subscriptions`) كانت موجودة من migration 003 كما في SCHEMA.md، وفارغة قبل هذه المرحلة. لاحظ: `questions` لا يحتوي `updated_at` (بتصميم SCHEMA.md — خلافًا لباقي جداول المحتوى).

### 11.3 منطق مشترك (`backend/src/lib/examGrading.js` — يستخدمه الدومينان)
- `getQuestionsForExam(examId, {revealCorrect})`: يرجع الأسئلة بترتيبها؛ مع `revealCorrect:false` تُخفى `is_correct` في الخيارات و`correct_answer` (حماية الإجابات قبل التسليم).
- `autoGrade(question, selectedOptionId)`: MCQ/صح-خطأ — خيار سليم = درجة السؤال كاملة، غير سليم = صفر؛ خيار لا يتبع السؤال ⇒ `null`.
- `attemptExpired(attempt)`: فحص **خادمي** للوقت `started_at + duration_minutes <= now` (حساب صحيح حتى لو عُدّل ساعة الجهاز)؛ `duration_minutes=NULL` = بلا حد.
- `recomputeAttempt(attemptId, {gradedBy})`: معاملة واحدة — الدرجة = مجموع `points_awarded` للمُصحَّح، `fullyGraded` (كل أسئلة الامتحان لها درجة) ⇒ `status='graded'` و `passed` = `score/total*100 >= pass_percent`، ثم `applyPassedAndUnlock` (تمرير = إدراج تقدم + فتح التالي؛ رسوب = حذف الصف الذي فتحه هذا التمرير).
- `lessonPassed` / `isLessonOpen`: تنفيذ قرار ب.

### 11.4 مسارات المدرس (`/api/teacher/` — `requireRole('admin')`)
| المسار | الوصف |
|---|---|
| `POST /exams` | إنشاء امتحان مرتبط بدرس: `title` (مطلوب)، `duration_minutes` ≥ 1، `pass_percent` افتراضية 50 (0–100)، `allow_retry` مطلوب، `max_attempts` اختياري (يُجبر `NULL` إن كانت `allow_retry=false`)، `status='published'` فورًا، `sort_order` تلقائي. |
| `POST /exams/:id/questions` | إضافة سؤال. MCQ: 4 خيارات بالضبط + `correct_index` 1–4. صح-خطأ: يُنشئ تلقائيًا خياري «صح»/«خطأ» مع `correct_answer='true'|'false'`. مقالي: `correct_answer` نموذجية اختيارية. `points` > 0. |
| `PUT /questions/:id` | تعديل النص/الدرجة/الشرح/الإجابة النموذجية، واستبدال خيارات MCQ، وقلب إجابة صح-خطأ (يُحدّث خياراته). **لا يمكن تغيير نوع السؤال** (400). |
| `DELETE /questions/:id` | حذف سؤال؛ **409** إن كان مرتبطًا بإجابات طلاب. |
| `GET /exams/essay-queue` | قائمة انتظار المقالي غير المُصحَّحة من محاولات `submitted`: نص الإجابة + نص السؤال + درجته + الإجابة النموذجية + اسم الطالب + توقيت التسليم (الأقدم أولًا). |
| `POST /answers/:id/grade` | تصحيح مقالي يدوي (نقاط من 0 إلى درجة السؤال، مرة واحدة فقط) ثم `recomputeAttempt` — إعادة احتساب الكل وفتح/إغلاق التالي تلقائيًا. Audit: `exam.answer_graded`. |
| `PUT /attempts/:id/score` | تعديل الدرجة النهائية (0..total). مرفوض لمحاولة `in_progress`. `passed` يُحسب فقط عند الاكتمال. Audit: `exam.attempt_score_override` بقيم before/after. |

### 11.5 مسارات الطالب (`/api/student/` — `requireRole('student')`)
| المسار | الوصف |
|---|---|
| `POST /exams/:id/attempts` | بدء محاولة. يتحقق: امتحان `published` + درس/كورس `published`، **اشتراك فعّال** في الكورس (غير منتهي)، **الدرس مفتوح**، حدود المحاولات (`allow_retry=0` ⇒ واحد، `max_attempts`)، لا محاولة حيّة غير منتهية، `total_points>0`. محاولة حية **منتهية الوقت** تُغلق تلقائيًا (تُسلَّم وتُحتسب) قبل بدء جديدة (audit `exam.attempt_auto_submitted`). يعود بالأسئلة مع **إخفاء الإجابات الصحيحة**. |
| `POST /attempts/:id/submit` | تسليم المحاولة (لصاحبها فقط). يرفض إن لم تكن `in_progress` أو انتهى وقتها **خادميًا**. يطالب بإجابة **كل** الأسئلة. MCQ/صح-خطأ تُصحَّح تلقائيًا، والمقالي يُسجل النص فقط (انتظار). ثم `recomputeAttempt`. |
| `GET /exams/:id/attempts` | تاريخ محاولات الطالب للامتحان (الأحدث أولًا): الحالة/الدرجة/الكل/النجاح/التواقيت. |
| `GET /attempts/:id` | مراجعة تفصيلية بعد التسليم فقط (400 إن كانت `in_progress`): الإجابات الصحيحة والخيار المختار لكل سؤال، نص المقالي، النقاط المكتسبة، طريقة التصحيح، توقيت التصحيح. |

### 11.6 قواعد ومنطق إضافي
- الاشتراك الفعّال: `subscriptions` بشرط `status='active'` و`expires_at IS NULL OR expires_at > now`.
- عدّاد المحاولات: المحاولات بحالة `submitted` أو `graded` فقط.
- تعليق الوقت: لا تسليم بعد انتهاء `started_at + duration` (فحص خادمي صريح)، والمحاولة المعلقة المنتهية تُغلق عند البدء التالي.
- كل عمليات الحساسة مسجلة في `audit_logs` بأفعال: `exam.created`, `question.created`, `question.updated`, `question.deleted`, `exam.attempt_started`, `exam.submitted`, `exam.attempt_auto_submitted`, `exam.answer_graded`, `exam.attempt_score_override`.

### 11.7 اختبار عملي (ضد الخادم الفعلي)
- **اختبار المدرس: 32/32** — إنشاء امتحان (النشر الفوري، `max_attempts` مجبرة عند إيقاف إعادة المحاولة)، رفض الدرس غير الموجود و`pass_percent>100`، الأسئلة الثلاثة (MCQ بتحقق 4 خيارات/فهرس صحيح، صح-خطأ بإنشاء خيارين، مقالي بنموذج)، رفض الدرجات ≤ 0، التعديل (نقاط/نص/استبدال خيارات/قلب صح-خطأ/منع تغيير النوع)، الحذف (نجاح + 404 + 409 مع إجابات)، قائمة الانتظار، التصحيح اليدوي مع إعادة الاحتساب (27/30 نجاح وفتح التالي، وإلغاء الفتح عند الرسوب)، حد الدرجة، تعديل النهائي (5 ⇒ رسوب + إغلاق التالي، 100 ⇒ 400)، والتدقيق في `audit_logs`.
- **اختبار الطالب: 34/34** — عزل المصادقة، رفض الاشتراك، قفل الدرس قبل تمرير السابق، بدء محاولة مع إخفاء الإجابات، رفض محاولة حية، رفض المراجعة قبل التسليم، رفض التسليم الناقص/الخيار الغير صالح/مقالي فارغ، التسليم مع توقف المقالي (15/30 بانتظار)، المراجعة بعد التسليم (كشف الصحيح + الاختيار)، منع التسليم المزدوج، المحاولة الثانية، تصحيح مقالي مع إعادة احتساب (رسوب ثم نجاح مع فتح التالي)، فتح درس الثاني بعد النجاح، رفض `allow_retry=0` وبلوغ `max_attempts=2`، انتهاء الوقت (رفض التسليم + إغلاق المحاولة المنتهية تلقائيًا عند بدء جديدة + سجل التدقيق)، ورفض امتحان `draft`.
- **E2E متكامل: 31/31** — قصة كاملة: بناء 3 دروس منشورة، 4 امتحانات (MCQ+صح-خطأ+مقالي / MCQ بلا إعادة / تصحيح تلقائي بالكامل / مقالي فقط)، **نجاح عند حد 50% بالضبط** (15/30) مع فتح الوحدة الثانية، **تعديل الدرجة لرسوب يغلق الثاني ويُمنع دخول امتحانه ثم إعادتها تفتحه**، محاولة فاشلة للثانية تغلق الثالثة، إعادة محاولة ناجحة تفتح الثالثة، امتحان مقالي فقط يُصحح ويُمرر، **قرار ج** (نجاح امتحان آخر لنفس الدرس لا يكرر صف التقدم)، منع إعادة محاولة، السماح بإعادة المحاولة دون حد أقصى، التاريخ والمراجعة، وحضور أفعال التدقيق الستة. كل الجداول عادت فارغة بعد التنظيف.

### 11.8 Commits
- `eb9d27a` — migration 007 (`allow_retry` + `student_lesson_progress`).
- `7469252` — `lib/examGrading.js` (تصحيح/احتساب/فتح/انتهاء) + اختباره.
- `5ade8af` — دومين المدرس: امتحانات وأسئلة وتصحيح مقالي وتعديل درجة نهائية.
- `0cab6d4` — دومين الطالب: بدء/تسليم/تاريخ/مراجعة.

### 11.9 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- **حالة الامتحان** (`draft`/`archived`) لم يُضف لها endpoint — الامتحانات تُنشأ `published` فورًا (لا يوجد "نشر لاحق" بعد).
- **إجابات تفاعلية أثناء الامتحان** (مؤقّت العد التنازلي على الواجهة، حفظ تلقائي، مؤشر أسئلة مُجابة) و**واجهات admin/client** لهذا النظام لم تُبنَ بعد — هذه المرحلة API فقط.
- **question_images** (صور داخل الأسئلة) موجودة في الجدول ولم تُستخدم بعد.

---

## 12) المرحلة السادسة — أكواد التفعيل والاشتراكات

نظام كامل للتفعيل بالكود: المدرس يولّد دفعات من أكواد قوية عشوائية مربوطة صرامةً بالكورس/السعر/الصلاحية، ويعرضها بفلترة وترقيم صفحات، ويحصل على تقرير المتاح لكل كورس، ويمكنه تفعيل كورس يدويًا لطالب محدد (بدون كود). الطالب يستبدل الكود بفحوص مرتبة برسائل عربية مميزة في معاملة ذرّية لا تُستغل مزدوجًا، مع تقييد صارم لكل مستخدم ولكل IP وتدقيق كل محاولة فاشلة، ويستعرض اشتراكاته النشطة.

### 12.1 قرارات التصميم
- **صيغة الكود:** على خلاف SCHEMA.md («12 رقمًا»)، اعتمد المستخدم **حروفًا وأرقامًا**: 12 محرفًا من أبجدية لا لبس فيها من 32 رمزًا (`ABCDEFGHJKLMNPQRSTUVWXYZ23456789` — بلا `0/1/I/O`)، أي ~2^60 من الاحتمالات. يُخزَّن كخيط واحد 12 محرفًا (فهرس فريد)، ويُعرض مجمعًا `XXXX-XXXX-XXXX` عبر `formatCode`، و`normalizeCode` يقرّب الاستبدال (حذف غير الأبجدية + أحرف كبيرة).
- **الربط صارم أبدي:** الكود يُولَّد مربوطًا بـ `course_id/stage_id/month_id/price` و`expires_at` ويرتبط بها نهائيًا — لا يتغير الربط لاحقًا.
- **قوة التوليد:** `crypto.randomBytes(12)` مع `byte % 32` (توزيع منتظم لأن 32 تقسم 256)، فحص تفرد محلي (Set) + فحص قاعدة البيانات لكل مرشح، داخل معاملة واحدة.
- **ذرّية الاستبدال:** كل فحوص الاستبدال داخل transaction واحدة في PostgreSQL، مع تحديث مشروط لحالة الكود وإنشاء الاشتراك ضمن نفس المعاملة.
- **تقييد الاستبدال:** طبقتان على `/redeem` — لكل مستخدم 5 محاولات / 15 دقيقة، ولكل IP 15 محاولة / 15 دقيقة (تُحتسب كل المكالمات بما فيها الناجحة). كل محاولة فاشلة (صيغة/غير موجود/مستخدم/ملغي/منتهي/مشترك مسبقًا/تجاوز حد) تُسجل في `audit_logs` بالسبب + الكود المُقرب، بما فيها عمليات الحظر 429 (منطق `res.status` مضمّن يسجّل مرة واحدة بالسبب الصحيح من الطبقة التي حظرت).
- **الاشتراك اليدوي مطابق للكود:** صف اشتراك حقيقي في `subscriptions` كاملًا كصف الكود، ولا يميّزه إلا حقل `source` (`activation_code` مع `activation_code_id` و`activated_by=NULL` مقابل `manual` مع `activated_by=admin` و`activation_code_id=NULL`).
- **المفعول الفعّال:** `expires_at=NULL` دائمًا (بلا تاريخ انتهاء)؛ «منتهي» يُحسب زمنيًا `status='unused'` مع `expires_at < now` في قائمة/تقرير المدرس.

### 12.2 مسارات المدرس (`/api/teacher/` — `requireRole('admin')`)
| المسار | الوصف |
|---|---|
| `POST /activation-codes/batch` | توليد دفعة: `course_id` (موجود)، `count` 1–1000، `price` عدد صحيح ≥ 0، `validity_days` ≥ 1، `batch_label` اختياري ≤ 200. يُشتق `stage_id`/`month_id` من الكورس (course.month_id → months.subject_id → subjects.stage_id). يُرجع الأكواد المنسقة + اسم الكورس + `expires_at`. Audit: `activation_codes.batch_generated`. |
| `GET /activation-codes` | القائمة: فلترة بكرس `course_id` أو حالة `unused/used/expired/revoked` (expired = محسوبة: unused ومنتهية زمنيًا)، ترقيم صفحات `page`/`page_size` (1..100) مع `total/total_pages` وضمّ اسم مستخدم الكود (`used_by_name`). |
| `GET /activation-codes/report` | تقرير بسيط لكل كورس له أكواد: `total_issued` + `available_count` (unused وغير منتهية) + `generated_at`. |
| `POST /subscriptions/manual` | تفعيل يدوي لطالب (دور student) لكورس: يرفض المستخدم غير الطالب/غير الموجود والكورس غير الموجود و**409** إن كان هناك اشتراك فعّال مسبق. `source='manual'`, `activated_by=admin`. Audit: `subscription.manual_activated` (before/after). |

### 12.3 مسارات الطالب (`/api/student/` — `requireRole('student')`)
| المسار | الوصف |
|---|---|
| `POST /redeem` | استبدال الكود بفحوص مرتبة برسالة مميزة: مطلوب → «الكود مطلوب»؛ صيغة خاطئة → «صيغة الكود غير صحيحة» (400)؛ غير موجود → «هذا الكود غير موجود» (404)؛ مستخدم → «هذا الكود مستخدم من قبل» (400)؛ مُلغى → «تم إلغاء هذا الكود» (400)؛ منتهي زمنيًا → «انتهت صلاحية هذا الكود» (400)؛ اشتراك فعّال مسبق لنفس الكورس → «أنت مشترك بالفعل في هذا الكورس» (409 **دون استهلاك الكود**). النجاح: معاملة واحدة تُنشئ الاشتراك (`source='activation_code'`, `activation_code_id`) وتقلب الكود `used` مع `used_by`/`used_at`. Audit: `activation_codes.redeemed`. |
| `POST /redeem` (تقييد) | `auditedLimiter` مزدوج: لكل مستخدم 5/15د ثم لكل IP 15/15د، كلاهما يعيد 429 + `Retry-After`، وكل فشل (بما فيه الحظر) مسجل `activation_codes.redeem_failed` بالسبب. |
| `GET /subscriptions` | اشتراكات الطالب النشطة فقط مع اسم/سعر الكورس و`source`/`activation_code_id`/`activated_by`/`activated_at`/`expires_at` (الأحدث أولًا). |

### 12.4 اختبار عملي (ضد الخادم الفعلي)
- **توليد الدفعات: 19/19** — رفض التحقق (عدم وجود كورس/عدد خارج 1–1000/سعر سالب/أيام صفرية/label طويل)، توليد دفعات متعددة في معاملة واحدة، تفرّد محلي + ضد قاعدة البيانات، `expires_at` = الآن + الأيام المحددة، فصل الصلاحية بين دفعات، عدم ازدواج بعد إعادة التشغيل، والتقاط `created_by`.
- **القائمة: 19/19** — ترقيم صفحات (أول/أخير/تجاوز النطاق يُثبَّت/إدخالات غير صالحة 400)، فلترة بالكورس والحالات الأربع، `effective_status` للكود منتهي، دمج المرشحات، وضمّ `used_by_name`.
- **التقرير: 8/8** — فارغ بداية، عدّادات صحيحة بعد استخدام كود ومنتهية كود آخر، و`generated_at`.
- **التفعيل اليدوي: 11/11** — رفض الطالب غير الموجود/غير الطالب/الكورس غير الموجود، إنشاء `source='manual'` مع `activated_by=admin`، 409 على التكرار، والتدقيق.
- **الاستبدال: 16/16** — كل الفحوص المرتبة برسائلها، نجاح كامل (اشتراك + كود used + بدون `activated_by`/`expires_at`)، إعادة الاستخدام مرفوضة، 409 لا يستهلك الكود الثاني، منتهي/مُلغى مرفوضان، والتدقيق الناجح.
- **التقييد والتدقيق: 19/19** — طالب 3: نجاح + فشل حتى الحد ثم 429 بطبقة المستخدم مع `Retry-After`؛ عزل المستخدم (طالب مؤقت جديد غير محظور)؛ 429 بطبقة IP عند استنفاد 15 مكالمة من نفس IP بغضّ النظر عن حصة المستخدم؛ سجل كل الأسباب (rate_limit_user ×2، rate_limit_ip ×1، invalid_format لكل محاولة صيغة، not_found)؛ واشتراك واحد فقط نشأ رغم كل الفشل.
- **قائمة الاشتراكات: 11/11** — فارغة بداية، اشتراكان (كود + يدوي) بنفس الأعمدة تمامًا ولا يميّزهما إلا `source`/`activation_code_id`/`activated_by`، واستبعاد المنتهي من القائمة النشطة.
- **E2E متكامل: 32/32** — القصة الكاملة بالمطالبة: توليد دفعة قوية (14 رمزًا XXXX-XXXX-XXXX من الأبجدية بلا 0/1/I/O، 2^60، مربوطة بالكورس/السعر/30 يومًا)، استبدال ناجح → اشتراك فعّال، إعادة استخدام مرفوضة، صيغة خاطئة وغير موجود برسالة واضحة، كود منتهٍ مرفوض، 409 مع بقاء الكود غير مستهلك، **الاشتراك اليدوي يظهر مطابقًا تمامًا لصف الكود في التقارير ولا يميّزه إلا `source`**، والقائمة/التقرير يعكسان كل شيء (مستخدم ×2 بأسماء الطلاب، متاح 1 من 3 بعد استخدام + انتهاء). قاعدة البيانات عادت فارغة بعد التنظيف.

### 12.5 Commits
- `d6dfbd1` — المدرس: توليد دفعات الأكواد (معاملة واحدة، تفرّد، ربط صارم).
- `0b771cd` — المدرس: قائمة الأكواد بترقيم صفحات + فلاتر (كورس/الحالات).
- `81c40e0` — المدرس: تقرير المتاح غير المستخدم لكل كورس.
- `1694eb3` — المدرس: التفعيل اليدوي بدون كود (صف اشتراك حقيقي `source='manual'`).
- `6d61157` — الطالب: استبدال الكود (فحوص مرتبة + معاملة ذرّية + منع الازدواج).
- `0011b06` — الطالب: تقييد استبدال صارم (مستخدم 5 / IP 15 لكل 15 دقيقة) + تدقيق كل فشل.
- `88e1258` — الطالب: قائمة الاشتراكات النشطة.

### 12.6 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- **لا بوابة دفع** بتوجيه صريح من المستخدم — الكود (أو التفعيل اليدوي) هو وسيلة الاشتراك الوحيدة.
- **لا إلغاء/تراجع اشتراك** في هذه المرحلة — الاشتراك النشط فريد لكل (طالب، كورس) بفهرس جزئي فريد؛ الإلغاء/الاسترداد ليسا ضمن المطلوب.
- **عدادات التقييد في الذاكرة** (تتصفر عند إعادة تشغيل الخادم) — كافية لطبيعة الاستبدال، ولا تتطلب تخزينًا دائمًا.
- **واجهات admin/client** لشاشات توليد/إدارة الأكواد والاستبدال لم تُبنَ بعد — هذه المرحلة API فقط.

---

## 13) المرحلة 7 — الجدولة التلقائية للنشر والإشعارات

### 13.1 قرارات التصميم (بتأكيد المستخدم)
1. **`publish_at` على كل جدول محتوى** (`courses`/`lessons`/`exams`) هو مصدر الحقيقة للجدولة؛ جدول `scheduled_publishes` **سجل تتبع فقط** (pending → done/cancelled) ولا يُقرأ منه المخطط.
2. **الإشعارات**: صف لكل طالب مستهدف (`user_id NOT NULL`) + عمودا `target_scope`/`target_stage_id` لتوثيق نطاق الإرسال؛ القراءة/غير المقروء لكل صف على حدة.
3. **عضوية المرحلة**: مشتقة من الاشتراكات النشطة (طالب له اشتراك فعّال في كورس ضمن المرحلة).
4. **إشعارات النشر** تُرسل لطلاب مرحلة المحتوى (كورس ← شهر ← مادة ← مرحلة) عند لحظة النشر الفعلي فقط.

### 13.2 قاعدة البيانات (migration 008 — فحوص PRAGMA)
- `lectures.publish_at` (نوع `TIMESTAMPTZ` في PostgreSQL).
- `notifications.target_scope TEXT NOT NULL DEFAULT 'user'` مع `CHECK IN ('all','stage','user')` + `notifications.target_stage_id INTEGER`.

### 13.3 البنية
- جدولة النشر تعتمد على `publish_at` وتوقيت PostgreSQL.
- `lib/notifications.js` — `dispatchNotification` (نقطة الإرسال الموحّدة الوحيدة) / `notifyUser` / `notifyContentPublished` / `getStageStudentIds` / `getContentContext`.
- `jobs/scheduler.js` — `runScheduledPublishes` (يفحص `status='draft'` و`publish_at <= now` للكورسات/الدروس/الامتحانات في دالة واحدة؛ كل صف في try/catch مستقل؛ تدقيق `${table}.scheduled_published` بـ`ip='scheduler'`؛ يعلّم السجل done ويُرسل الإشعار) + `startPublishScheduler` (نبضة فورية عند الإقلاع ثم `setInterval`).
- `server.js` — تشغيل المجدول بعد `listen` بفاصل `PUBLISH_SCHEDULER_INTERVAL_MS` (الافتراضي 60 ثانية).
- `content.js` (مدرس) — حقول `publish_at` للكورسات/الدروس + `POST/DELETE /:key/:id/schedule` + استدعاء `afterContentState` بعد الإنشاء/التعديل/تغيير الحالة.
- `exams.js` (مدرس) — إنشاء الامتحان مع `publish_at` (يبدأ draft) + `POST/DELETE /exams/:id/schedule` + `afterContentState`.
- `notifications.js` (مدرس) — إرسال يدوي بنطاقات (all/stage/user).
- `notifications.js` (طالب) — قائمة إشعاراته + تعليم كمقروء.
- `codes.js` (مدرس/طالب) — إشعار عند التفعيل اليدوي (`subscription_activated`) وعند استبدال الكود (`code_redeemed`).

### 13.4 نقاط النهاية
| المسار | الوصف |
|---|---|
| `POST /api/teacher/{courses\|lessons}/:id/schedule` | جدولة نشر (يتطلب draft؛ يضبط `publish_at` + سجل pending + تدقيق `${table}.schedule_set`). |
| `DELETE /api/teacher/{courses\|lessons}/:id/schedule` | إلغاء الجدولة (يمسح `publish_at` + سجل cancelled + يبقى draft). |
| `POST /api/teacher/exams/:id/schedule` / `DELETE ...` | نفس المنطق للامتحانات (`exams.schedule_set` / `exams.schedule_cancelled`). |
| `POST /api/teacher/notifications` | إرسال يدوي: `scope='all'` (كل الطلاب النشطين)، `scope='stage'` + `stage_id`، `scope='user'` + `user_id` (يجب أن يكون طالبًا). يكتب صفًا لكل مستلم داخل معاملة + تدقيق `notifications.sent`. |
| `GET /api/student/notifications` | إشعارات الطالب (الأحدث أولًا، بحد 200) مع `unread_count`. |
| `POST /api/student/notifications/:id/read` | تعليم كمقروء (404 إن لم يكن لصاحبه). |

### 13.5 اختبار عملي (ضد الخادم الفعلي)
- **خطوة 1–7**: كل خطوة اختُبرت مع إعادة تشغيل الخادم وتنظيف كامل بعدها — التحقق من أعمدة 008 بالـ PRAGMA، النشر التلقائي لدروس/كورسات/امتحانات مجدولة، تعديل موعد الجدولة وإلغاؤها (يبقى draft + سجل cancelled)، النشر الفوري عبر تغيير الحالة يُشعِر الطلاب، قائمة الإشعارات وترتيبها و`unread_count` وعلامة القراءة (404 لمن ليس مالكه)، الإرسال اليدوي (الكل/المرحلة/طالب) وتسجيل `target_scope`/`target_stage_id`، وإشعارا التفعيل اليدوي والاستبدال.
- **E2E متكامل 31/31**: قصّة كاملة عبر الـ API الحقيقي بمخطط فاصل 3 ثوانٍ — كورس A مجدول ثم عدّل موعده ثم نُشر تلقائيًا (سجل done)؛ درس A جُدول ثم أُلغي (يبقى draft، سجل cancelled)؛ درس B وامتحان مجدولان نُشرا تلقائيًا؛ امتحان فوري بلا `publish_at` نُشر مباشرة؛ تدقيق `scheduled_published` للأنواع الثلاثة؛ الطالب (id=3) استلم إشعارات أنواع النشر الخمسة بعناوين/نصوص عربية؛ القراءة قلّصت `unread_count`؛ الإرسال اليدوي أنشأ 2/2/1 بالمقابل وسُجّل النطاق؛ إشعارا التفعيل اليدوي والاستبدال؛ والقاعدة عادت فارغة بعد التنظيف.
- **ملاحظة إصلاح بيانات**: الهاش الأصلي لحساب محمود عبد (id=3) فُقد أثناء الكتابة فوق ملف الحفظ المؤقت في دورات اختبار E2E، فأُعيد ضبطه لكلمة مرور موثقة `test1234`؛ وأُصلح حساب أحمد محمد (id=2) ليطابق توثيق `student123` (كان متبقيًا من خطأ تنظيف المرحلة 6). تحقّقنا بتسجيل دخول فعلي (200) لكل منهما.

### 13.6 Commits
- `fec6d22` — migration 008 (courses/exams publish_at + أعمدة نطاق الإشعارات).
- `211cc0d` — مجدول النشر المشترك + مكتبتا notifications/publish + تشغيله من server.js.
- `c227142` — حقول publish_at الموحّدة للكورسات/الدروس + نقاط الجدولة/الإلغاء.
- `7526c12` — publish_at للامتحانات + جدولة/إلغاء + ربط afterContentState.
- `ab063e9` — الطالب: قائمة الإشعارات + تعليم كمقروء.
- `f182f83` — المدرس: إرسال إشعار يدوي (all/stage/user).
- `9c7b84e` — إشعارا التفعيل اليدوي واستبدال الكود.

### 13.7 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- المخطط يفحص فقط `status='draft'` و`publish_at <= now`؛ لا إشعار عند *الجدولة* بل عند *النشر الفعلي* فقط.
- الإشعارات حاليًا داخل النظام فقط (in-app)؛ `dispatchNotification` هي نقطة الإرسال الوحيدة لإضافة بريد/دفع لاحقًا دون لمس بقية الكود.
- عدادا التقييد في الذاكرة كما في المرحلة 6؛ إشعارات الإرسال اليدوي للطلاب النشطين في لحظة الإرسال فقط.
- ربط واجهات admin/student بكل هذا (شاشات الجدولة والإشعارات) لم يُبنَ بعد — المرحلة API فقط.

---

## 14) المرحلة 8 — لوحة تحكم المدرس (`frontend/admin/`) + endpoints قراءة فقط

### 14.1 قرارات التصميم (بتأكيد المستخدم)
1. **لا منطق أعمال جديد في الواجهة** — كل عملية تذهب للـ API الموجود؛ أُضيفت **4 endpoints قراءة/حالة فقط** لسد فجوات كان مستحيلًا ربط الواجهة بدونها (إحصاءات النظرة العامة، قائمة/ملف/حظر الطلاب، نتائج الامتحانات). اختيار المستخدم: "إضافة endpoints قراءة فقط (موصى به)".
2. **8 أقسام بشريط جانبي ثابت + أعلى شريط**: نظرة عامة، إدارة المحتوى والكورسات، إضافة محاضرة وامتحان، إدارة ومتابعة ملفات الطلاب، أكواد التفعيل، الامتحانات والدرجات، الإشعارات، الملف الشخصي — بالعربية، RTL، ألوان العلامة (ذهبي `#D4AF37` على بني داكن).
3. **تسلسل التصحيح المقالي**: قائمة انتظار المقالي تُصفَّح من الأقدم، اعتماد الدرجة يستدعي `POST /answers/:id/grade` ويعيد الحساب التلقائي (تحوّل المحاولة إلى graded + فتح الدرس التالي إن نجح).
4. **تسلسل إضافة المحاضرة**: إنشاء درس ← (اختياري) رفع فيديو ← مرفقات ← نشر فوري/مجدول ← إنشاء امتحان ← إضافة الأسئلة (3 أنواع ببنّاء أسئلة).
5. **إدارة المحتوى الهرمية**: شريط تنقّل فئات ← مراحل ← مواد ← شهور ← كورسات ← دروس مع عدّاد أبناء لكل بطاقة، ترتيب، تغيير حالة، حذف مؤكَّد (يرفض الخادم المرتبط بمحتوى أعمق).

### 14.2 البنية الجديدة
- `backend/src/domains/teacher/students.js` — ملف جديد: قائمة/ملف/حظر/نتائج/إحصاءات.
- `backend/src/domains/teacher/exams.js` — `GET /exams` (قائمة + فلتر `lesson_id` + `question_count`).
- `backend/src/domains/teacher/index.js` — ربط `studentsRoutes`.
- `frontend/admin/index.html` — أُعيدت كتابته كاملة: 8 أقسام + 3 مودالات (`contentFormModal`, `manualActivateModal`, `codesModal`) + `#toast`.
- `frontend/admin/style.css` — كتلة "PHASE 8 ADDITIONS" (شريط تنقّل، شارات، ترقيم صفحات، toast، بطاقات المحتوى/المقالي، صندوق نتائج الأكواد، إلخ).
- `frontend/admin/teacher-script.js` — وحدات مستقلة لكل قسم (initShell/Overview/Content/AddContent/Students/Codes/Exams/Notifications/Profile) + مساعدات (esc/toast/api/statusBadge/fmtDT/renderPagination/goToSection/closeModal).

### 14.3 نقاط النهاية الجديدة
| المسار | الوصف |
|---|---|
| `GET /api/teacher/students` | قائمة طلاب (فلتر `q`/`status`/`stage_id` + `page`/`page_size`، عدّاد الاشتراكات النشطة لكل طالب). |
| `GET /api/teacher/students/:id` | ملف الطالب الكامل: بياناته + اشتراكاته + محاولاته + إحصاءات (كورسات/محاولات/ناجحة/دروس). |
| `PATCH /api/teacher/students/:id/status` | حظر/إلغاء حظر (active/banned)؛ الحظر يقطع الجلسات الفعّالة ويسجل `student.status_changed`؛ يرفض تغيير حالة الحساب الذاتي. |
| `GET /api/teacher/attempts` | نتائج المحاولات بفلتر (exam_id/course_id/student_id/status/passed) + ترقيم صفحات، بمعلومات الامتحان/الدرس/الكورس/الطالب. |
| `GET /api/teacher/stats` | عدادات النظرة العامة (طلاب/مشتركون/محتوى منشور/أكواد متاحة ومستخدمة/مقالي بانتظار التصحيح) + أحدث 6 طلاب. |
| `GET /api/teacher/exams` | قائمة الامتحانات مع عدد أسئلة كل امتحان (بفلتر `lesson_id` اختياري) — يخدم إدارة المحتوى والنتائج. |

### 14.4 اختبار عملي (ضد الخادم الفعلي)
- **الإحصاءات/الطلاب**: /stats، /students، /students/:id، بحث `q=محمود`، ban→revert، منع الحظر الذاتي، رفض دخول الطالب الموقوف.
- **المحتوى**: تسلسل فئة→مرحلة→مادة→شهر→كورس→درس بـ curl، نشر كورس، ترتيب (ورفض ترتيب خارج النطاق برسالة عربية)، رفع غلاف PNG، إنشاء امتحان + أسئلة MCQ/صح-خطأ/مقالي، وعدّ `question_count`.
- **الوسائط**: رفع فيديو mp4 ✓، مرفق PDF ✓، ورفض png كمرفق ("صيغة الملف غير مسموح بها") — صيغ المرفقات: pdf/docx/doc/pptx/ppt/xlsx/xls/txt/zip (حتى 20MB، حتى 10 ملفات).
- **إضافة المحتوى E2E**: درس + فيديو + مرفق + نشر + امتحان بأسئلته — ثم تنظيف كامل.
- **التصحيح E2E (31/31 نمط):** كورس منشور ← تفعيل يدوي ← دخول الطالب (أحمد id=2) ← بدء المحاولة ← تسليم (mcq خاطئ + مقالي) ← المحاولة `submitted` بلا نتيجة ← ظهورها في قائمة المقالي النموذجية ← اعتماد 5/5 ← المحاولة `graded` 5/7 و`passed=1` ← قائمة انتظار المقالي صفرت — ثم تنظيف كامل (بما فيه جلسات الطالب والتقدم).
- **أكواد**: توليد دفعة 5 أكواد، فلترة، تقرير لكل كورس.
- **إشعارات**: الكل (2 مستلم)، لطالب (1)، رفض مرحلة غير موجودة، رفض بدون عنوان.
- **فحص نهائي**: `node --check` سليم + كل 119 معرّفًا مستخدمًا في الـ JS موجودًا في الـ HTML (عدا `contentCoverInput` الديناميكي داخل المودال) + كل وحدات init معرّفة ومستدعاة.

### 14.5 Commits
- `ace0656` — backend: endpoints قراءة/حالة للطلاب والنتائج والإحصاءات + GET /exams.
- `e9d0d16` — frontend: هيكل اللوحة (شريط + 8 أقسام + مودالات) + نظرة عامة مربوطة بـ /stats.
- `3c9b054` — إدارة المحتوى الهرمية (تصفح/إضافة/تعديل/ترتيب/حالة/حذف/غلاف).
- `9fd959d` — إضافة محاضرة وامتحان (فيديو/مرفقات/بنّاء أسئلة 3 أنواع).
- `16f938d` — الطلاب (بحث/فلترة/ملف/حظر/تفعيل يدوي).
- `d0488ef` — أكواد التفعيل (توليد/فلترة/تقرير لكل كورس).
- `4139842` — الامتحانات والدرجات (نتائج بفلاتر + قائمة تصحيح المقالي).
- `4b87ef4` — الإشعارات (الكل/مرحلة/طالب).

### 14.6 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- الواجهة تعتمد على أكواد الاستجابة الموحّدة `{message}` من الخادم لعرض الأخطاء العربية كما هي.
- الحذف المادي للامتحان لا يزال بلا endpoint (لا يُحذف الامتحان من الواجهة؛ يكفي أرشفة المحتوى).
- التنقل المباشر بين قوائم المحتوى يعتمد على جلب كل مستويات الشجرة دفعة واحدة عند فتح القسم (صغير الحجم حاليًا).
- بوابة الطالب `frontend/client/` وربط شاشات الامتحانات/الوسائط للطالب ما زالت عملًا قادمًا مقترحًا.

---

## 15) المرحلة 9 — بوابة الطالب (`frontend/client/`) + endpoints قراءة/تحديث

### 15.1 قرارات التصميم (بتأكيد المستخدم)
1. **خيار المستخدم: "إضافة endpoints قراءة فقط + تحديث حساب"** — البوابة مربوطة بالـ API الفعلي، وأُضيفت endpoints قراءة/حالة فقط لسد فجوات (كتالوج متاح، تفاصيل كورس مشترك به مع حالة قفل الدروس، محتوى درس مع إحصاءات امتحاناته، قائمة المحاولات، ملف الطالب).
2. **إعادة كتابة كاملة** للبوابة: 5 أقسام بالعربية/RTL/سمة ذهبي-بني مطابقة لهوية اللوحة، بلا أي Mock (البيانات الثابتة القديمة أُزيلت).
3. **الفتح التسلسلي للدروس** يُعرض بالاعتماد على `lesson_status` (`open`/`passed`) المحسوب خادميًا من `isLessonOpen`/`lessonPassed` — الواجهة لا تعيد تنفيذ المنطق.
4. **بيانات تجريبية** لاختبار E2E كامل، مع التنظيف الكامل بعدها (تم).

### 15.2 نقاط النهاية الجديدة (`/api/student/` — `requireRole('student')`)
| المسار | الوصف |
|---|---|
| `GET /courses` | كتالوج الكورسات المنشورة وغير المشترك بها: أسماء المرحلة/المادة/الشهر + `lessons_count` (المنشورة فقط). |
| `GET /courses/:id` | تفاصيل كورس مشترك به فعليًا: معلوماته + دروسه (`sort_order`/`lesson_status` = `open`/`passed`/`locked`) + `progress: {total_lessons, passed_lessons, completed_percent}`. |
| `GET /courses/:id/lessons/:lessonId` | محتوى درس واحد: دروس + فيديوهات/ملفات (`lesson_assets`) + امتحانات مع `attempts_count`/`passed`/`best_percent`/`latest_attempt`؛ **403** للدرس غير المفتوح أو غير المنتمي للكورس. |
| `GET /attempts` | كل محاولات الطالب (الأحدث أولًا) مع سياق الكورس/الدرس/الامتحان. |
| `GET /profile` | ملف الطالب: `users` (name/email/phone/avatar) + `student_profiles` (student_number/guardian_name/guardian_phone/governorate/birth_date/notes). |
| `PATCH /profile` | تحديث حقول الملف (الاسم/ولي الأمر/المحافظة/تاريخ الميلاد/ملاحظات/رقم الطالب) + رسائل عربية وتدقيق. |

### 15.3 الواجهة (المرحلة 9)
- `frontend/client/index.html` — أُعيدت كتابته: 5 أقسام (الكورسات المتاحة + مودال استبدال كود، الكورسات المشترك بها مع خماسية مستويات والتقدم، النتائج والمحاولات + مراجعة الامتحان، الدعم الفني، الحساب الشخصي) + إشعارات.
- `frontend/client/script.js` — كل الأقسام مربوطة بـ endpoints حقيقية؛ `frontend/client/style.css` أُضيفت له نهاية "Phase 9 Student Portal Additions" (الملف 1551 سطرًا).

### 15.4 اختبار عملي (ضد الخادم الفعلي)
- **Endpoints**: كتالوج بلا اشتراك، تفاصيل مشترك به (درس أول فقط `open`)، محتوى درس مع إحصاءات، 403 لدرس مقفول/غير منتمٍ، قائمة المحاولات، ملف + تحديثه.
- **E2E كامل بطالب جديد**: تسجيل ← كتالوج ← استبدال كود ← درس أول فقط (0%) ← امتحان (MCQ+صح-خطأ+مقالي) ← تصحيح المدرس للمقالي ← فتح الثاني (33%) ← امتحان MCQ تلقائي ← ناجح ← فتح الثالث (67%) ← مراجعة المحاولات/الإشعارات/البروفايل.
- **حماية الصفحات**: `/client` بلا جلسة → 302 لـ `/login.html`، وبجلسة → 200؛ الأصول (style.css/script.js) = 200.
- Commits: `d7df7ab` (endpoints)، `5e648a5` (UI).

### 15.5 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- مودال استبدال الكود يطلب الكود ثم يفعل الاشتراك؛ الشاشات المدفوعة/الـ payment خارج النطاق (الاشتراك بالكود أو التفعيل اليدوي فقط).
- بيانات الاختبار التنظيفية: حُذفت كل صفوف المحتوى/الاشتراكات/المحاولات/الطلاب التجريبيين بعد المرحلة 10.

---

## 16) المرحلة 10 — ربط الصفحات العامة بالبيانات الحقيقية والسلوك

### 16.1 قرارات التصميم (بتأكيد المستخدم عبر استبيان)
1. **قاعدة صارمة**: لا تغيير/إعادة بناء للتصميم البصري للصفحات العامة (`index.html`, `about.html`) — الربط عبر **JavaScript فقط** (ملف جديد `assets/js/home-data.js` + سطر `<script>` واحد). كل الأقسام/الألوان/الخطوط كما هي.
2. **عرض المراحل**: الصفحة الرئيسية لم تكن تحتوي قسم مراحل، فقُرّر (خيار المستخدم) **حقن قسم "المراحل الدراسية" عبر JS** قبل قسم الدورات المميزة، بنفس كلاسات/أنماط الكروت والزر الذهبي الموجودة، مع زر "سجّل الآن" لكل مرحلة.
3. **تخزين رسائل التواصل**: جدول جديد `contact_messages` (migration 009) لرسائل الزوار غير المسجلين — بدل إجبار `tickets.user_id` (خيار المستخدم).
4. **إعادة التوجيه**: حفظ الوجهة المقصودة في معامل `?redirect=` بصفحة الدخول، مع منع الـ open-redirect (يُقبل فقط مسار نسبي يبدأ بـ `/` ولا يبدأ بـ `//`). لو مسجل دخولًا → الوجهة مباشرة.

### 16.2 قاعدة البيانات (migration `009`)
- جدول `contact_messages(id, name, email, subject, message, created_at)` + فهرس على `created_at` (رسائل زوار بلا ربط بمستخدم).

### 16.3 نقاط النهاية العامة (`/api/courses/` — بلا جلسة)
| المسار | الوصف |
|---|---|
| `GET /public/home` | `{ stages, featured }`: مراحل منشورة بترتيب `sort_order` مع `courses_count` (كورسات منشورة داخل المرحلة) + كورسات `status='published'` و`badge='featured'` بأسماء المرحلة/المادة/الشهر و`price` و`cover_url` بالترتيب الصحيح (لا تُعرض إلا لمنشور كامل السلسلة). |
| `POST /public/contact` | التحقق (اسم/بريد صالح/عنوان/رسالة، حدود أطوال)، يخزّن في `contact_messages` + تدقيق `contact.message_received` + تقييد `createRateLimiter` (10/ساعة/IP) مع `Retry-After`. |

### 16.4 سلوك الواجهة
- **المراحل + الكورسات المميزة**: `home-data.js` يجلب `/public/home` ويرسم القسمين ديناميكيًا (كروت بنفس الأنماط، صورة غلاف أو الصورة الافتراضية عند غيابها/فشلها).
- **أزرار "سجّل الآن"/"اشترك الآن"**: لو غير مسجل → `/login.html?redirect=%2Fclient%2Findex.html` (الوجهة المحفوظة)، ولو مسجل → `/client/index.html` مباشرة.
- **نموذج التواصل**: يعترض الإرسال (يستبدل عقدة النموذج لإلغاء ربط `php-email-form` القديم بـ `forms/contact.php`) ويرسل JSON إلى `/public/contact` ويشغّل `.loading/.error-message/.sent-message` الموجودة.
- **صفحة الدخول (`login.html`)**: تقرأ `?redirect=` وتوجّه إليه بعد نجاح الدخول/التسجيل (الافتراضي `/client/index.html`)، مع منع الروابط الخارجية.
- **`pageGuard` (الخادم)**: عند إعادة التوجيه لحارس الصفحات المخصصة يمرر الوجهة: `/login.html?redirect=<originalUrl>` — فتعود أي روابط عميقة (مشاهدة درس مثلًا) للوجهة بعد الدخول.

### 16.5 اختبار عملي (E2E ضد الخادم الفعلي)
- بيانات تجريبية (3 مراحل منشورة، كورسات منشورة بعضها `featured` بأسعار/أغلفة فعلية من `assets/img/illustration/`) ثم:
- زائر غير مسجل: `GET /` = 200 مع `home-data.js`، `/public/home` يعيد 3 مراحل مرتبة + 3 مميزة مرتبة.
- زر سجّل/اشترك → `/login.html?redirect=%2Fclient%2Findex.html` = 200.
- تسجيل حساب جديد (`POST /auth/register`) → جلسة → `GET /client/index.html` = 200 (العودة للوجهة).
- رابط عميق كضيف: `GET /client/index.html` → 302 لـ `/login.html?redirect=...` → تسجيل دخول → الوجهة = 200.
- مسجل دخولًا: `/client/` = 200 مباشرة.
- نموذج التواصل: رفض بريد غير صالح برسالة عربية، نجاح يخزّن الصف + تدقيق، وتقييد 10/ساعة/IP (429 بعد الحد).
- **تنظيف كامل** عاد بقاعدة البيانات للحالة الموثقة (users 1/2/3 فقط وكل الجداول فارغة).

### 16.6 Commits
- `4ec5fa6` — backend: endpoints عامة (`/api/courses/public/home` + `POST /public/contact`) + migration 009 (`contact_messages`) + `pageGuard` يمرر `?redirect=`.
- `e3c97dd` — frontend: `home-data.js` (مراحل/مميزة/أزرار/نموذج التواصل) + `login.html` يدعم `?redirect=`.

### 16.7 ملاحظات (خارج نطاق المرحلة — موثقة فقط)
- `about.html` صفحة تعريفيّة ثابتة بلا بيانات من قاعدة البيانات — لم يُربط (لا شيء يربط).
- الوجهة النهائية للأزرار هي `/client/index.html` (لا توجد صفحة كورس عامة لكل كورس)؛ المتابعة تُكمل من كتالوج الطالب.
- حصص التقييد للتواصل في الذاكرة (تُتصفر عند إعادة تشغيل الخادم) — كافٍ لنموذج تواصل.
- عدادا تقييد الاستبدال (المرحلة 6) لم تُمسّ — يظلان كما هما.

---

## 17) المرحلة 11 — التقارير + SEO + البحث الشامل + المراجعة الأمنية

### 17.1 المطلوب والمنجز

- **تقارير المدرس الحقيقية** (إيرادات من اشتراكات وأكواد مُستخدمة، مصروفات، صافي ربح، تصدير Excel عربي RTL).
- **SEO شامل** للصفحات العامة فقط (صفحة كورس مُنتجة من السيرفر، sitemap ديناميكي، robots، OG meta ديناميكي).
- **بحث شامل** (طالب داخل اشتراكاته + معلم عبر طلاب/كورسات/دروس/أكواد/اشتراكات/مصروفات).
- **مراجعة أمنية شاملة** لكل الدومينات + إصلاحات + `SECURITY_REVIEW.md`.

### 17.2 قرارات التصميم (بموافقة المستخدم)

1. **مصادر الإيرادات:** `COALESCE(ac.price, c.price)` — سعر الكود المُستخدم (إن وُجد) أو سعر الكورس (للتفعيل اليدوي). من صف `subscriptions` النشط فقط.
2. **بنية التقسيم:** 5 محاور (فئة تعليمية / مرحلة / مادة / شهر / كورس) عبر JOIN بـ `educational_categories → stages → subjects → months → courses`.
3. **Excel عبر `exceljs`** (موافقة صريحة بالتثبيت — استثناء لقاعدة "ممنوع npm install"). UUID transitive مُعالَج بـ `overrides`.
4. **صفحة الكورس من السيرفر** (`GET /course/:id`) — خيار المستخدم الرسمي (كراولز واتساب/فيسبوك لا تنفذ JS).
5. **حقن OG meta** عبر middleware يقرأ `PAGE_META` ويُحقن قبل `</head>` (لا helm/helmet إضافي، استجابة نقية).
6. **`robots.txt`** يستثني `/admin` `/client` `/api` `/uploads` وصفحات الدخول.
7. **بحث الطالب:** حصرها على ما اشترك به فعليًا (`WHERE course_id IN (...)` على اشتراكاته النشطة).
8. **بحث المعلم:** ست خانات (students/courses/lessons/codes/subscriptions/expenses) كلها LIKE-escaped.

### 17.3 التقارير والـ exports — endpoints

| المسار | الوصف |
|---|---|
| `GET /api/teacher/reports/revenue` | إجمالي + متوسط + تقسيم حسب الفئة/المرحلة/المادة/الشهر/الكورس (مع فلتر `from`/`to` اختياري). |
| `POST /api/teacher/expenses` | تسجيل مصروف: `category` (≤200)، `amount` (int ≥ 1)، `expense_date` (YYYY-MM-DD)، `description` (≤2000). Audit. |
| `GET /api/teacher/expenses` | قائمة بفلتر `from`/`to` (حساب تاريخي على `expense_date`). |
| `GET /api/teacher/reports/profit` | `revenue - expenses` (نفس فلتر التاريخ). |
| `GET /api/teacher/reports/export?type=students\|revenue\|subscriptions` | Excel عربي RTL: `views:[{rightToLeft:true, frozen:ySplit:1}]`، ترويسة `#2C1E0F`/`#D4AF37`، أرقام كقيم رقمية، Content-Type الصحيح. |

### 17.4 SEO — نقاط النهاية والـ middleware

- `GET /course/:id` — صفحة HTML كاملة (Bootstrap + Cairo + OG/Twitter/canonical) من DB مع `esc()`. `Content-Type: text/html; charset=utf-8`، `Cache-Control: public, max-age=300`. 404 لغير المنشور.
- `GET /sitemap.xml` — `urlset` يتضمن `/` + `/about.html` + `/course/<id>` لكل كورس منشور (`lastmod` من `updated_at`).
- `GET /robots.txt` — Disallow للمناطق الحساسة + Sitemap.
- `app.injectMeta` — middleware يقرأ `PAGE_META` (ثوابت) ويحقن OG/canonical/Twitter قبل `</head>` (يستخدم `readCache` لمنع إعادة الحقن).

### 17.5 البحث — endpoints

| المسار | الوصف |
|---|---|
| `GET /api/student/search?q=` | حصر على اشتراكات الطالب النشطة فقط. LIKE-escaped. تقييد 30/د لكل مستخدم. |
| `GET /api/teacher/search?q=` | 6 خانات: students/courses/lessons/codes/subscriptions/expenses. LIKE-escaped. تقييد 60/د لكل مستخدم. |

### 17.6 المراجعة الأمنية — الإصلاحات الـ 4

| # | الـ Commit | الإصلاح |
|---|---|---|
| 1 | `2de53da` | **استخراج امتداد الرفع من sniff magic:** `storage.js` يقبل `extension` صريح و`media.js` يحوّل عبر `SNIFF_TO_EXT`. حلّ Content-Type confusion من `.html` المُزيَّف. |
| 2 | `573ba14` | **SESSION_SECRET مطلوب في الإنتاج + cookie.secure من NODE_ENV** — يرفض البدء بدونه في prod. |
| 3 | `93af69b` | **Global Express error handler + process-level uncaught handlers** — رسالة عربية عامة، لا stack. |
| 4 | `2fb4458` | **Raxs headers** (`X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, HSTS in prod). |

### 17.7 اختبار E2E شامل (ضد الخادم الفعلي)

**تقارير + Excel (16 سيناريو):**
- Reports: revenue/expenses/profit للمعلم + 401 للطالب + 400 للأدخالات الخاطئة + فلاتر from/to.
- Excel: students (7080B) / revenue (7140B) / subscriptions (7100B) بـ RTL/freeze/header gold+brown، 400 لـ `type=hack`، 401 للجلسة الخاطئة.
- Audit: `report.exported` لكل تحميل.

**SEO (8 سيناريو):**
- `/course/1` 200 + OG/canonical/RTL/CSS + هروب صحيح للأسماء.
- `/course/999` → 404 + `/course/abc` → 404.
- `/sitemap.xml` ديناميكي (3 كورسات + 2 صفحة).
- `/robots.txt` يستثني المناطق الحساسة.
- Homepage و`/index.html` و`/about.html` و`/login.html` تحوي OG tags (1 لكل صفحة، لا تكرار عبر طلبين).

**البحث (8 سيناريو):**
- طالب 2 يبحث "نحو" → كورس + امتحان (اشتراكه). يبحث "بيـان" → فارغ (خارج اشتراكه).
- طالب 3 يبحث "بيـان" → درس (اشتراكه). يبحث "نحو" → فارغ.
- معلم يبحث "محمد" → طالب، "ABC" → كود، "إيجار" → مصروف، "الإعراب" → درس.
- Rate limit على `/api/student/search`: بلغ 429 عند الطلب 29 (30/د).

**الأمان (23 سيناريو) — كل النتائج في `SECURITY_REVIEW.md` §6:**
- طالب → `/api/teacher/*` = 401 (4 نقاط).
- معلم → `/api/student/*` = 401 (2 نقاط).
- طالب 3 → إشعار طالب 2 = 404.
- طالب 3 → محاولة طالب 2 = 403.
- معلم يحاول حظر نفسه = 400.
- حقن XSS في `name` → يُخزّن كـ text (آمن عبر `esc()` في العرض).
- `NODE_ENV=production` بدون SESSION_SECRET = exit 1.
- رفع `fake.html` (PNG بايتات) → حفظ كـ `.png` → `Content-Type: image/png`.
- رفع `.html` كمرفق = 400 (allowlist).
- Headers: `X-Content-Type-Options: nosniff` + `Referrer-Policy` + `X-Frame-Options: SAMEORIGIN` + لا `X-Powered-By` + HSTS في prod.
- JSON غير صالح = 500 مع رسالة عامة (لا stack).
- Rate limit على البحث = 429.
- 0 vulnerabilities في `npm audit`.

### 17.8 Commits

- `b249ae8` — phase11: teacher reports (revenue/expenses/profit).
- `13fe5ef` — phase11: real Arabic RTL Excel export (exceljs) + uuid override.
- `8404bab` — phase11: SEO (server-rendered /course/:id + sitemap + robots + OG meta injection).
- `26abbba` — phase11: student search (scoped to own subscriptions).
- `ace453a` — phase11: teacher unified search.
- `2de53da` — phase11: security — upload extension from sniffed magic.
- `573ba14` — phase11: security — cookie.secure + SESSION_SECRET guards.
- `93af69b` — phase11: security — global error handler + process handlers.
- `2fb4458` — phase11: security — basic security headers.
- `945627a` — phase11: SECURITY_REVIEW.md.

### 17.9 ملاحظات (خارج النطاق — موثّقة)

- **SITE_URL** افتراضي `http://localhost:8000` — اضبطه في الإنتاج عبر ENV.
- **HSTS** يُرسل فقط في الإنتاج (`NODE_ENV=production`).
- **exceljs** أُضيف كاستثناء بقاعدة "ممنوع npm install" — الـ `overrides` لـ `uuid@^11.1.1` يُغلق GHSA-w5hq-g745-h8pq.
- **rate limit buckets** في الذاكرة (تُصفّر مع restart) — كافٍ للنسخة الحالية.
- **trust proxy** غير مضبوط (localhost مباشرة) — اضبطه عند وضع reverse proxy.
- **UI لوحات المعلم/الطالب** لا تستهلك بعد endpoints البحث/التقارير/SEO العامة (يمكن إضافتها لاحقًا).

### 17.10 الخلاصة

- **كل الأجزاء الـ 4 من المرحلة 11 مكتملة.** ✅
- بيانات اختبار حقيقية في DB (4 مراحل، 2 كورس، 2 درس، 1 امتحان، 1 كود مُستخدم، 2 اشتراك، 3 مصروفات) — تُظهر عمل التقارير/SEO/البحث. للتنظيف: `node -e "..."` (موثّق في الـ commits).
- **SECURITY_REVIEW.md** تقرير أمني شامل مع 23 محاولة اختراق فعلية.
- **النظام جاهز للنشر** بعد تطبيق Production Checklist داخل `SECURITY_REVIEW.md` §7.1.
