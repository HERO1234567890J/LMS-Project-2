# SCHEMA.md - PostgreSQL database schema

هذه الوثيقة تصف قاعدة البيانات التي ينفذها التطبيق حاليًا في
`backend/src/schema.js`.

## قاعدة البيانات والاتصال

- قاعدة البيانات المستخدمة: **PostgreSQL**.
- الاتصال يتم من خلال مكتبة Node.js `pg` في `backend/src/db.js`.
- رابط الاتصال يقرأ من المتغير `DATABASE_URL`.
- القيمة الافترية المحلية هي قاعدة `law_lms` على `localhost:5432`.
- تشغيل `npm run migrate` ينفذ `schemaSql`، وهو آمن لإعادة التشغيل لأن الجداول تستخدم `IF NOT EXISTS`.
- لا توجد طبقة ORM أو مجلد migrations منفصل؛ تعريف الجداول موجود مباشرة في `backend/src/schema.js`.
- جلسات `express-session` تحفظ في PostgreSQL من خلال `connect-pg-simple`.

## أنواع PostgreSQL المستخدمة

- `SERIAL`: مفاتيح أساسية رقمية ذاتية الزيادة.
- `INTEGER`: أرقام تعريف وترتيب وأحجام.
- `TEXT`: النصوص والروابط والأكواد.
- `NUMERIC(10,2)`: الأسعار والدرجات الرقمية.
- `BOOLEAN`: القيم المنطقية مثل الحالة والنجاح.
- `DATE`: تاريخ ميلاد الطالب.
- `TIMESTAMPTZ`: التواريخ والأوقات مع المنطقة الزمنية.
- `JSONB`: خيارات أسئلة الامتحان.
- `pgcrypto`: يتم تفعيله في بداية الـ schema عند الحاجة.

## الجداول والعلاقات

### `users`

حسابات المستخدمين، وتشمل المدير والطلاب. يحتوي الجدول على الهاتف والبريد وكلمة المرور المشفرة والدور والحالة وتواريخ الدخول.

### `students`

ملف إضافي للطالب بعلاقة واحد إلى واحد مع `users` عبر `user_id`.
يحتوي على المرحلة، رقم الطالب، بيانات ولي الأمر، المحافظة وتاريخ الميلاد.

### الهيكل التعليمي

العلاقة الهرمية هي:

`levels` -> `terms` -> `subjects` -> `lectures` -> `lecture_files`

- `levels`: الفرق أو المراحل الدراسية.
- `terms`: الترم الأول أو الثاني.
- `subjects`: المادة، اسم الدكتور، السعر، الغلاف والوصف وحالة النشر.
- `lectures`: محاضرات المادة، ترتيبها، روابط الفيديو وموعد النشر.
- `lecture_files`: ملفات المحاضرات المرفقة.
- `lectures.bunny_video_id`: معرّف فيديو Bunny Stream عند استخدامه بدل التخزين المحلي.

### الاشتراكات وأكواد التفعيل

- `activation_codes`: كود فريد مرتبط بمادة، مع السعر وتاريخ الانتهاء وحالة الاستخدام أو الإلغاء.
- `subscriptions`: اشتراك الطالب في المادة، ويرتبط اختياريًا بكود التفعيل.
- يوجد قيد `UNIQUE(student_id, subject_id)` لمنع تكرار اشتراك الطالب في المادة نفسها.

### الامتحانات

العلاقة هي:

`exams` -> `exam_questions` -> `exam_attempts` -> `exam_answers`

- `exams`: امتحان المحاضرة، نسبة النجاح، المدة وعدد المحاولات.
- `exam_questions`: أسئلة من نوع `mcq` أو `true_false` أو `essay`، مع الخيارات بصيغة `JSONB` عند الحاجة.
- `exam_attempts`: محاولات الطلاب ودرجاتهم وحالتهم.
- `exam_answers`: إجابات الطالب، التصحيح، الدرجة وطريقة التصحيح.

### التقدم والإشعارات

- `lesson_progress`: يربط الطالب بالمحاضرات المكتملة.
- `notifications`: إشعارات عامة أو حسب المرحلة أو لمستخدم محدد.
- `notification_reads`: يتتبع قراءة كل مستخدم للإشعار.

## الحذف والعلاقات

- حذف مستخدم يحذف ملف الطالب المرتبط به (`ON DELETE CASCADE`).
- حذف مادة يحذف محاضراتها وأكوادها واشتراكاتها التابعة حسب تعريف العلاقات.
- حذف محاضرة يحذف ملفاتها وامتحاناتها وتقدمها.
- حذف امتحان يحذف أسئلته ومحاولاته.
- حذف محاولة يحذف إجاباتها.
- بعض العلاقات لا تستخدم `ON DELETE CASCADE`، ولذلك يطبق PostgreSQL السلوك الافتراضي لمنع حذف سجل ما زالت سجلات أخرى تعتمد عليه.

## التهيئة والتشغيل

من مجلد `backend`:

```bash
npm install
npm run migrate
npm run seed
npm start
```

يجب ضبط `DATABASE_URL` و`SESSION_SECRET` في ملف `.env` قبل تشغيل التطبيق.